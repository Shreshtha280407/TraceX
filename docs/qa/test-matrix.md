# Phase 1 Test Matrix

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| P4-RELEASE-GATE-001 | Team | All six Phase 4 paths, replay, lease security, migrations, full suite, and dedicated Compose synthetic E2E | `uv run pytest`; `docker compose -p tracex-phase4-gate --env-file .env.example up --build -d` | Blocked | Python/migration gates pass; dedicated API image build is blocked by Docker-build DNS lookup failures for PyPI, so Compose E2E is not claimed. See 2026-09-14 results. |
| P4-MEDIA-ORCH-001 | Nipun | Deterministic manifests; duplicate-free chunk replay; partial observations/artifacts/checkpoints retain exact scope | `uv run pytest -q tests/unit/evidence_lifecycle/test_media_orchestration.py` | Passing | Focused synthetic fixture run; no media decoding/GPU required |

## Phase 3 final integration acceptance

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| P3-INTEGRATION-001 | Nipun | All producer batches, lifecycle, security, and graph replay remain coherent | `uv run pytest -q` | Passing | 1656 passed, 1 skipped; `docs/qa/phase-3-acceptance.md` |
| P3-MEDIA-422-001 | Nipun/Gaurav | Multiple frame OCR progress never regresses at the aggregate media batch | `uv run pytest -q tests/unit/media_processing/test_media_worker_orchestration.py` | Passing | Regression and real-Tesseract live test passed |

Each row is a tracked QA item: what it verifies, who owns it, how to run it, and where the current result is recorded. Actual run output/history lives in `docs/qa/test-results.md` — this file defines the matrix, that file records outcomes.

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| GRAPH-MAPPING-P3-001 | Shreshtha | Deterministic document/CDR/finance plans; explicit incomplete/unsupported results; no I/O or entity resolution | `uv run pytest -q tests/unit/graph/test_mapping.py` | Passing | `docs/qa/test-results.md` |
| GRAPH-MAPPING-P3-002 | Shreshtha | Live case scope, evidence traversal, event-first temporal shape, repeat idempotency | `uv run pytest -q tests/integration/graph/test_phase_3_mapping_live.py` | Passing | `docs/qa/test-results.md` |

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| CORE-CONTRACT-001 | Nipun | Every canonical contract (`EvidenceRecordV1`, `ObservationV1`, `EntityV1`, `EventV1`, `WorkerJobV1`, `WorkerResultV1`) parses a valid fixture, round-trips through JSON without semantic change, and rejects an unsupported `schema_version`. | `uv run pytest tests/contract/test_evidence.py tests/contract/test_observation.py tests/contract/test_entity.py tests/contract/test_event.py tests/contract/test_worker.py` | Passing | See `docs/qa/test-results.md` |
| CORE-CONTRACT-002 | Nipun | Field-level contract validation rules hold: empty `source_locator` rejected; `time_end_ms < time_start_ms` rejected; confidence outside `[0,1]` rejected; invalid/degenerate normalized bounding box rejected; `EventV1` requires `event_time` or `time_window`; malformed worker idempotency key rejected; `WorkerResultV1` status/error consistency enforced. | `uv run pytest tests/contract/test_common.py tests/contract/test_observation.py tests/contract/test_event.py tests/contract/test_worker.py` | Passing | See `docs/qa/test-results.md` |
| CORE-ID-001 | Nipun | `deterministic_uuid` is stable for identical normalized input, changes when input changes, and doesn't collide across part boundaries; `canonical_bytes`/`canonical_sha256` are stable regardless of dict/key insertion order and handle UUID/datetime/enum/nested-model values deterministically. | `uv run pytest tests/unit/test_ids.py tests/unit/test_canonical.py` | Passing | See `docs/qa/test-results.md` |
| CORE-CONFIG-001 | Nipun | Missing required settings (`POSTGRES_DSN`, `NEO4J_URI`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`, `REDIS_URL`, `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`) or an invalid value (e.g. malformed DSN) fails `Settings()` construction with a clear `ValidationError`, not a downstream crash. | `uv run pytest tests/unit/test_config.py` | Passing | See `docs/qa/test-results.md` |
| CORE-HEALTH-001 | Nipun | `GET /healthz` returns `200` with `{"status": "ok", "service": "tracex-api", "version": ...}`, and the FastAPI app assembles and serves routes on startup (no dependency required). | `uv run pytest tests/unit/test_api_health.py::test_healthz_returns_200 tests/unit/test_app_startup.py` | Passing | See `docs/qa/test-results.md` |
| CORE-READY-001 | Nipun | `GET /readyz` returns `200` with per-dependency `"ok"` status when all of PostgreSQL/Neo4j/Redis/MinIO are healthy (mocked), and `503` with the safe `{"error": {...}}` envelope — never a connection string, credential, or stack trace — when any dependency fails. | `uv run pytest tests/unit/test_api_health.py tests/security/test_no_secret_leakage.py` | Passing | See `docs/qa/test-results.md` |
| CORE-COMPOSE-001 | Nipun | `compose.yaml` is syntactically valid and resolvable by Docker Compose (`docker compose config`). | `uv run pytest tests/integration/test_compose_config.py` (or directly `docker compose config`) | Passing | See `docs/qa/test-results.md` |
| CORE-MIDDLEWARE-001 | Aditya | `RequestIDMiddleware` and `SecurityHeadersMiddleware` (both pure ASGI) pass non-HTTP scopes (`lifespan`) straight through untouched, clean up correlation-ID context in `finally`, and never leak that context into a later request. | `uv run pytest tests/unit/test_error_handling.py -k "lifespan or context"` | Passing | See `docs/qa/test-results.md` |
| CORE-ERROR-ENVELOPE-001 | Aditya | A route-raised `HTTPException`, a dependency-raised `HTTPException`, a request-validation failure, and a genuinely unexpected exception (route or dependency) each return the project's safe JSON error envelope and the intended status code — never an `ExceptionGroup`, `BaseExceptionGroup`, or other framework-internal detail — and a valid request's success response/status is unchanged. | `uv run pytest tests/unit/test_error_handling.py` | Passing | See `docs/qa/test-results.md` |
| CORE-REQUEST-CONTEXT-001 | Aditya | The `X-Request-ID` correlation header is present (caller-supplied value honored, otherwise generated) on success responses, on every handled-error response, and on the one response path (`unhandled_exception_handler`, dispatched by Starlette's outermost `ServerErrorMiddleware`) that no user-added middleware ever sees — verified as the specific case the original design missed. | `uv run pytest tests/unit/test_error_handling.py -k request_id` | Passing | See `docs/qa/test-results.md` |
| SECURITY-ERROR-SAFETY-001 | Aditya | No error response (handled or unexpected) ever contains a stack trace, `ExceptionGroup` text, exception class name, secret, password, token, or connection string; `app/modules/access_control/`'s register/login/refresh/logout/`/me` and protected-access-control tests all still pass with the module's local `_internal_error` workaround removed, relying solely on the central handler. | `uv run pytest tests/unit/test_error_handling.py tests/security/test_no_secret_leakage.py tests/security/access_control/test_auth_no_secret_leakage.py tests/unit/access_control tests/security/access_control` | Passing | See `docs/qa/test-results.md` |
| GRAPH-SCHEMA-001 | Shreshtha | Every graph constraint/index statement uses `IF NOT EXISTS` and gives `Evidence`/`Observation`/`Entity`/`Event` a compound `(case_id, <domain>_id)` uniqueness constraint (never the domain ID alone, never `IS NODE KEY` which is Enterprise-only); applying schema twice against live Neo4j succeeds and `verify_schema` reports every statement present. | `uv run pytest tests/unit/graph/test_schema.py tests/integration/graph/test_schema_apply.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROJECTION-001 | Shreshtha | Evidence/Observation/Entity/Event projection writes only allow-listed, JSON-safe properties (never raw `attributes`/document text), uses parameterized Cypher (never string-interpolated IDs/labels), is MERGE-based (idempotent), and creates only the Case/Evidence/Observation/Entity/Event relationships defined in `docs/architecture/graph-taxonomy-v1.md` — never a direct entity-to-entity edge. | `uv run pytest tests/unit/graph/test_projection.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROJECTION-002 | Shreshtha | A referenced dependency that hasn't been projected yet (`Evidence` for an `Observation`, or any `Entity` for an `Event`'s participants) produces a safe, explicit `DEFERRED` result naming exactly what's missing, and writes nothing at all — atomically, with no partial node/relationship. | `uv run pytest tests/unit/graph/test_projection.py -k defer` | Passing | See `docs/qa/test-results.md` |
| GRAPH-ISOLATION-001 | Shreshtha | The same-looking domain ID (e.g. a reused `entity_id`) projected into two different cases never shares graph identity; a case-scoped query for one case cannot retrieve another case's event/entity data. | `uv run pytest tests/unit/graph/test_projection.py -k case_id tests/integration/graph/test_case_isolation.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROVENANCE-001 | Shreshtha | Every projected `Observation` retains its `evidence_id` and `source_locator`; `get_evidence_provenance`/`get_observation_provenance` return the exact evidence/source-locator references needed to deep-link to supporting material. | `uv run pytest tests/unit/graph/test_projection.py -k source_locator tests/integration/graph/test_projection_and_queries.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-QUERY-001 | Shreshtha | Every public graph-query method requires `case_id` (rejecting missing/empty), filters `case_id` on every `MATCH` — not only the anchor node — and bounds/paginates any multi-row result. | `uv run pytest tests/unit/graph/test_queries.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-INTEGRATION-001 | Shreshtha | Against a live Compose Neo4j instance: schema applies twice cleanly, a minimal evidence→observation→entity→event fixture projects to the exact expected labels/relationships, repeated projection creates no duplicates, and case isolation holds end to end. Self-skips (never fabricates a pass) if Neo4j is unavailable. | `uv run pytest tests/integration/graph -v` | Passing (live) | See `docs/qa/test-results.md` |
| DOC-PROCESS-001 | Jasraj | Content is classified by content-type + extension (never extension alone); native-text PDF, DOCX (paragraphs + text-bearing table cells), and TXT (UTF-8 with a documented Latin-1 fallback) all extract deterministically; `fir_report_text_v1`'s fixed regex patterns detect every documented mention type (FIR reference, police station, phone, email, vehicle plate, UPI/account/transaction reference, currency-prefixed amount, legal section, date/time) only when explicitly labelled or structurally distinctive. | `uv run pytest tests/unit/structured_processing/test_classifier.py tests/unit/structured_processing/test_txt.py tests/unit/structured_processing/test_pdf.py tests/unit/structured_processing/test_docx.py tests/unit/structured_processing/test_fir_report.py` | Passing | See `docs/qa/test-results.md` |
| DOC-PROVENANCE-001 | Jasraj | Every emitted `ObservationV1` carries the exact `page`/`span_start`/`span_end`/`row`/`sheet`/`column`/`json_path` provenance for its source format; `observation_id` is stable for identical `(case_id, evidence_id, profile, observation_type, locator)` input and changes when the locator, profile version, or observation type changes. | `uv run pytest tests/unit/structured_processing/test_provenance.py tests/unit/structured_processing/test_worker_dispatch.py::test_same_job_produces_same_observation_ids_across_runs` | Passing | See `docs/qa/test-results.md` |
| DOC-OCR-ROUTING-001 | Jasraj | A scanned/image-only PDF page is never given fabricated text; a fully-scanned PDF defers the whole job (`status=DEFERRED`, `observations=[]`, a deterministic `document_requires_ocr` checkpoint); a partially-scanned PDF still defers overall but includes full observations for the pages that did have embedded text; an encrypted PDF fails as `encrypted_pdf_unsupported` without ever attempting a password (including an empty one). | `uv run pytest tests/unit/structured_processing/test_pdf.py tests/unit/structured_processing/test_worker_dispatch.py -k pdf` | Passing | See `docs/qa/test-results.md` |
| CDR-NORMALISE-001 | Jasraj | `cdr_generic_v1` resolves documented header aliases case/space/hyphen-insensitively; normalizes a phone number only when unambiguous (Indian mobile), otherwise retains the original; parses timestamps only against documented formats; rejects a record missing `caller_number`/`timestamp` (or with an unparseable timestamp) as `required_field_missing`; never creates an `EventV1`, graph relationship, or person identity. | `uv run pytest tests/unit/structured_processing/test_cdr.py` | Passing | See `docs/qa/test-results.md` |
| FINANCE-NORMALISE-001 | Jasraj | `financial_transaction_generic_v1` preserves monetary source values exactly (original string kept alongside a `decimal.Decimal`-normalized value, never a `float`); requires an explicit, never-invented currency; rejects a record missing `amount`/`currency` or with an unparseable amount; never infers that two accounts share an owner or creates a criminal-network link. | `uv run pytest tests/unit/structured_processing/test_finance.py` | Passing | See `docs/qa/test-results.md` |
| STRUCTURED-SAFETY-001 | Jasraj | CSV/XLSX/JSON parsing never evaluates a spreadsheet formula (a formula cell's raw formula string is read as text); documented size/row/column/cell-length/JSON-depth/JSON-node limits are enforced; malformed CSV/XLSX/JSON fails safely with a named error code; DOCX/XLSX ZIP containers are validated (entry count, uncompressed size, compression ratio) before being opened by `python-docx`/`openpyxl`. | `uv run pytest tests/unit/structured_processing/test_csv_parser.py tests/unit/structured_processing/test_xlsx_parser.py tests/unit/structured_processing/test_json_parser.py tests/unit/structured_processing/test_limits.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-RESULT-001 | Jasraj | `process_job` never imports a PostgreSQL/Neo4j/Redis/MinIO/queue client (static AST check across every file in the module) and never imports `EntityV1`/`EventV1` (no entity/event construction, ever); error messages never echo sensitive extracted values (phone numbers, amounts); every emitted observation validates as `ObservationV1` and every result validates as `WorkerResultV1`, including round-tripping through JSON. | `uv run pytest tests/unit/structured_processing/test_safety.py tests/unit/structured_processing/test_worker_dispatch.py` | Passing | See `docs/qa/test-results.md` |
| AUTH-REGISTER-001 | Aditya | Registration hashes passwords with Argon2id (never stores/returns plaintext or the hash), rejects invalid email/short passwords safely (no submitted value ever echoed back, even alongside another invalid field), normalizes email deterministically, rejects a duplicate email with `409`, and never grants elevated privilege automatically. | `uv run pytest tests/unit/access_control/test_password.py tests/unit/access_control/test_service.py -k register tests/unit/access_control/test_api.py -k register` | Passing | See `docs/qa/test-results.md` |
| AUTH-LOGIN-001 | Aditya | Login success issues a valid access token + opaque refresh token; unknown email, disabled user, and wrong password all render the identical generic `401` (body *and* timing, via a dummy Argon2id verification); an access token rejects bad signature/issuer/audience/expiry/type/malformed claims. | `uv run pytest tests/unit/access_control/test_service.py -k login tests/unit/access_control/test_tokens.py tests/unit/access_control/test_api.py -k login` | Passing | See `docs/qa/test-results.md` |
| AUTH-REFRESH-001 | Aditya | Refresh rotates the token every time (old session revoked, new session in the same token family); a rotated-away token replayed revokes the *entire* family, including its still-active leaf session; every denial reason renders as the identical generic `401`. | `uv run pytest tests/unit/access_control/test_sessions.py tests/unit/access_control/test_api.py -k refresh` | Passing | See `docs/qa/test-results.md` |
| AUTH-LOGOUT-001 | Aditya | Logout revokes the session behind a refresh token and is idempotent (an unknown or already-revoked token is not an error, safe to call repeatedly). | `uv run pytest tests/unit/access_control/test_sessions.py -k logout tests/unit/access_control/test_api.py -k logout` | Passing | See `docs/qa/test-results.md` |
| AUTH-RATE-LIMIT-001 | Aditya | Login/refresh are rate-limited by a privacy-safe hashed key (never raw email/IP) over a documented fixed window; exceeding it returns a safe generic `429`; a Redis outage fails *closed* (denies, same as an over-limit hit) rather than silently allowing unlimited attempts. | `uv run pytest tests/unit/access_control/test_rate_limit.py tests/unit/access_control/test_api.py -k rate_limit` | Passing | See `docs/qa/test-results.md` |
| RBAC-CASE-001 | Aditya | The role/action matrix (`case_owner`/`case_manager`/`investigator`/`analyst`/`reviewer`/`viewer` × `case_read`/`case_manage`/`evidence_read`/`evidence_write`/`graph_read`/`review_decide`/`export_case_data`) permits and denies exactly the documented actions; a membership from one case cannot access a different case; an inactive membership or inactive user denies; a missing `case_id` denies; an unrecognized role/action denies (default deny). | `uv run pytest tests/unit/access_control/test_policy.py` | Passing | See `docs/qa/test-results.md` |
| ABAC-CLEARANCE-001 | Aditya | Ordered clearance (`restricted` < `confidential` < `secret`) denies access when a member's clearance ranks below the case's classification, even with a fully-permitting role; case membership/clearance decisions reflect live PostgreSQL rows, not cached values (deactivating a membership row denies on the very next read). | `uv run pytest tests/unit/access_control/test_policy.py -k clearance` | Passing | See `docs/qa/test-results.md` |
| SECURITY-MIDDLEWARE-001 | Aditya | `X-Content-Type-Options`/`X-Frame-Options`/`Referrer-Policy` appear on every response; `Cache-Control: no-store` appears on every `/api/v1/auth/*` response including error paths; no permissive `Access-Control-Allow-Origin` is ever set; request-validation errors never echo a submitted value (closing a real FastAPI default-handler leak for a body containing a password); an unexpected backend failure never leaks a connection string or stack trace. | `uv run pytest tests/unit/access_control/test_api.py -k "header or cache_control" tests/security/access_control/test_auth_no_secret_leakage.py` | Passing | See `docs/qa/test-results.md` |
| SECURITY-AUDIT-001 | Aditya | `security_audit_events` records safe telemetry only (event type, outcome, request ID, nullable user/case ID, an IP hash, safe metadata) and never a password, access token, refresh token, password hash, or raw evidence/CDR/financial value — checked across API responses, audit rows, and captured logs for register/login (success and failure)/refresh/logout. | `uv run pytest tests/security/access_control/test_auth_no_secret_leakage.py` | Passing | See `docs/qa/test-results.md` |
| RETRY-SAFETY-001 | Aditya | The retry helper retries only an explicitly allowed, transient-failure-prone operation (bounded exponential backoff with jitter) and *never* retries `login`/`register`/`password_change`/`token_rotation`/`session_revocation`/`review_decision`/`evidence_write` — verified by asserting the wrapped operation is never even invoked for those names. | `uv run pytest tests/unit/access_control/test_retry.py` | Passing | See `docs/qa/test-results.md` |
| LAN-RUNBOOK-001 | Aditya | `docs/runbooks/lan-development.md` documents a three-laptop LAN demo setup with only the API port exposed, PostgreSQL/Neo4j/Redis/MinIO kept LAN-inaccessible via host firewall (no `compose.yaml` port-binding changes), a `/healthz`/`/readyz` LAN-reachability check, and an explicit "not production" warning. | Manual review of `docs/runbooks/lan-development.md` | Passing | See `docs/qa/test-results.md` |
| AUDIO-METADATA-001 | Sarthak | `audio/metadata.py` extracts only deterministic WAV technical facts (duration, sample rate, channels, sample width, frame count, codec) via the stdlib `wave` module — no `ffmpeg`/subprocess; a non-RIFF/WAVE file, a truncated file (declared frames not backed by enough bytes), an oversized file, and an over-duration file all fail safely with a named error code, never partial/fabricated metadata. | `uv run pytest tests/unit/communication_processing/test_audio_metadata.py` | Passing | See `docs/qa/test-results.md` |
| AUDIO-PROVENANCE-001 | Sarthak | Every audio-derived `ObservationV1` (WAV metadata, imported transcript segment, imported diarization segment) carries exact `time_start_ms`/`time_end_ms` (+ `json_path` for segments) provenance; identical normalized input yields identical `observation_id` across repeated calls; transcript `extraction_confidence` is the caller-supplied value passed through unchanged, never invented. | `uv run pytest tests/unit/communication_processing/test_transcript_import.py tests/unit/communication_processing/test_diarization_import.py tests/unit/communication_processing/test_communication_worker_dispatch.py -k determinis` | Passing | See `docs/qa/test-results.md` |
| AUDIO-DEFER-001 | Sarthak | A request for `transcript_import_v1`/`diarization_import_v1` given only raw audio (no already-produced segments) returns `WorkerStatus.DEFERRED` with a `deferred_requires_asr`/`deferred_requires_diarization` checkpoint — never `SUCCEEDED` with fabricated text or speaker turns; a non-WAV audio format routes to `unsupported_audio_format`, never invented metadata. | `uv run pytest tests/unit/communication_processing/test_audio_routing.py tests/unit/communication_processing/test_communication_worker_dispatch.py -k defer` | Passing | See `docs/qa/test-results.md` |
| DIARIZATION-BOUNDARY-001 | Sarthak | `speaker_label` is source-local only (`entity_type_hint="speaker_label_local"`, never `"person"`/verified-identity wording); overlapping speaker turns are preserved, never merged or discarded; invalid segments (bad time range, out-of-range confidence, empty/overlong label) fail safely with `invalid_diarization_segment`. | `uv run pytest tests/unit/communication_processing/test_diarization_import.py` | Passing | See `docs/qa/test-results.md` |
| CHAT-PARSER-001 | Sarthak | WhatsApp plain-text, Telegram JSON, Instagram JSON, and generic social-intelligence JSON all parse their documented shape correctly (sender/text/timestamp fields, Telegram `reply_to_message_id`, service-message skipping); malformed, over-nested, oversized, and unsupported-shape input all fail safely with a named error code rather than partially succeeding. | `uv run pytest tests/unit/communication_processing/test_whatsapp.py tests/unit/communication_processing/test_telegram.py tests/unit/communication_processing/test_instagram.py tests/unit/communication_processing/test_json_records.py tests/unit/communication_processing/test_chat_safety.py` | Passing | See `docs/qa/test-results.md` |
| CHAT-PROVENANCE-001 | Sarthak | Every chat-derived `ObservationV1` carries non-empty source provenance (`span_start`/`span_end` for WhatsApp, `json_path` for the three JSON-based formats); a timestamp normalizes to UTC only when the source is unambiguous by construction (explicit offset/`Z`, or a Unix epoch value) — an ambiguous/naive timestamp keeps `timestamp_raw` only and is never guessed. | `uv run pytest tests/unit/communication_processing/test_whatsapp.py -k span tests/unit/communication_processing/test_telegram.py -k unixtime tests/unit/communication_processing/test_instagram.py -k timestamp tests/unit/communication_processing/test_json_records.py -k timestamp` | Passing | See `docs/qa/test-results.md` |
| CHAT-SAFETY-001 | Sarthak | No message/handle/phone/token/full-export content ever appears in a `ProcessingError` message; formulas/code/templates/expressions in export content are never evaluated (chat text and JSON values are inert data only). | `uv run pytest tests/unit/communication_processing/test_chat_safety.py -k sensitive tests/unit/communication_processing/test_module_safety.py` | Passing | See `docs/qa/test-results.md` |
| ALIAS-NORMALIZATION-001 | Sarthak | NFC Unicode normalization is idempotent and unifies composed/decomposed forms; `comparison_key` is case- and whitespace-insensitive and deterministic; `detect_script` correctly classifies Latin, Devanagari, Gurmukhi, mixed, and unknown (unsupported-script/digit-only) text. | `uv run pytest tests/unit/communication_processing/test_aliases_normalize.py tests/unit/communication_processing/test_aliases_scripts.py` | Passing | See `docs/qa/test-results.md` |
| ALIAS-TRANSLITERATION-001 | Sarthak | Devanagari/Gurmukhi transliteration via the small documented character-table algorithm is stable and deterministic (worked examples: राम -> `raama`, सिंह -> `sinha`); a token with any character outside the table yields `category="not_generated"`, never a partial/invented transliteration; phone numbers, emails, URLs, handles, and digit-bearing IDs are never transliterated as names. | `uv run pytest tests/unit/communication_processing/test_aliases_transliteration.py` | Passing | See `docs/qa/test-results.md` |
| COMM-LINK-001 | Sarthak | `CommunicationLinkCandidate`s are produced only for the five documented deterministic reasons, always `status=proposed_for_review`, with real `evidence_refs` pointing at the two source observations; `candidate_id` is deterministic and order-independent for the same pair, and changes when the pair or link type changes; a candidate is never constructed from display-name or transliteration similarity alone (`LinkableMessageDescriptor` has no such field at all). | `uv run pytest tests/unit/communication_processing/test_linking.py` | Passing | See `docs/qa/test-results.md` |
| COMM-ISOLATION-001 | Sarthak | Every link-candidate function requires all input descriptors to share one `case_id`, rejecting a mixed-case batch outright (`cross_case_input_rejected`) rather than silently splitting or partially processing it; every `ObservationV1` this module emits carries the `case_id`/`evidence_id` from the `WorkerJobV1` it was built for. | `uv run pytest tests/unit/communication_processing/test_linking.py -k cross_case tests/unit/communication_processing/test_communication_worker_dispatch.py -k case` | Passing | See `docs/qa/test-results.md` |
| COMM-WORKER-001 | Sarthak | `process_job` never imports a PostgreSQL/Neo4j/Redis/MinIO/queue/HTTP/subprocess/ML client (static AST check across every file in the module) and never imports `EntityV1`/`EventV1`; every result is a real, frozen-contract-valid `WorkerResultV1` (including round-tripping through JSON); a `FAILED`/`DEFERRED` result's `error`/`checkpoint` never contains a filename, handle, phone number, or other sensitive value. | `uv run pytest tests/unit/communication_processing/test_module_safety.py tests/unit/communication_processing/test_communication_worker_dispatch.py tests/integration/communication_processing` | Passing | See `docs/qa/test-results.md` |
| COMM-INTERCHANGE-001 | Sarthak | `parse_transcript_import_payload`/`parse_diarization_import_payload` accept the documented `{"segments": [...]}` JSON interchange shape (optional `language_hint` on transcript segments only) and reject malformed UTF-8, invalid JSON, a missing top-level `segments` array, a missing required field, and a wrong field type — all as `malformed_json_payload`, before any timing/bounds validation runs; timing/bounds validation still runs downstream unchanged. | `uv run pytest tests/unit/communication_processing/test_transcript_import.py -k parse_transcript_import_payload tests/unit/communication_processing/test_diarization_import.py -k parse_diarization_import_payload` | Passing | See `docs/qa/test-results.md` |
| COMM-WORKER-CLIENT-001 | Sarthak | `WorkerApiClient` (this module's own, `httpx`-based) speaks Nipun's internal-API wire format exactly against an `httpx.MockTransport`: claim success/no-work/`401`/processor-scope `403`/`503`, submit success/`409`/`422`/`401`, and `fetch_input` success/oversized-rejected/`404`; the worker token and claim token never appear in a captured log line. | `uv run pytest tests/unit/communication_processing/test_communication_worker_client.py` | Passing | See `docs/qa/test-results.md` |
| COMM-WORKER-ORCHESTRATION-001 | Sarthak | `run_once` claims through each `SUPPORTED_PROCESSORS` entry in turn; on a real claim it resolves input, verifies SHA-256 before ever calling `process_job` (mismatch submits `FAILED`/`evidence_integrity_mismatch`), builds the correct typed `InputPayload` for `audio_metadata_v1`/`transcript_import_v1`/`diarization_import_v1`/`whatsapp_export_v1` (a malformed interchange payload submits `FAILED` via `_build_input_payload`'s own `ProcessingError` handling, before `process_job` is ever called), calls the unchanged `process_job`, and submits the result; an `InputResolutionUnavailableError` submits `DEFERRED`/`input_resolution_unavailable`; the claim token is never logged; `main`'s `--once` CLI wiring returns `1` when `WORKER_TOKEN` is unconfigured and `0` on a clean no-job exit. | `uv run pytest tests/unit/communication_processing/test_communication_worker_orchestration.py` | Passing | See `docs/qa/test-results.md` |
| COMM-WORKER-LIVE-001 | Sarthak | Against a live Compose stack: the real `WorkerApiClient` reaches the real running internal API and gets a genuine "no work"/auth-rejection response back; a second, parametrized test proves the *complete* path for both live-reachable profiles (`audio_metadata_v1` via `SourceType.AUDIO`, `generic_social_json_v1` via `SourceType.CHAT`) — a real case/user/evidence upload followed by a real `run_once()` completes claim -> stream -> parse -> submit, producing a genuine `SUCCEEDED` result with real, case-scoped observations, confirmed independently via the job-status API. Provisions its own dedicated worker-credential token (distinct from `structured_processing`'s live test's token) so the two suites' credential scopes never collide in a shared full-suite run. Self-skips entirely if there's no `.env`, the live server/PostgreSQL/MinIO aren't reachable, or `WORKER_TOKEN` isn't configured. | `uv run pytest tests/integration/communication_processing/test_communication_worker_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| COMM-BATCH-001 | Sarthak (Phase 3) | `run_communication_job_with_batches` reuses `_dispatch` unchanged, submits bounded micro-batches (`Settings.communication_batch_size`) through `client.submit_batch` with real `TransformationProvenanceV1`/`ObservationBatchProgressV1` (scoped to that batch's own observations only), renews its lease via `client.renew_lease` every 5 batches, and always ends with exactly one terminal `WorkerResultV1` (`observations=[]` on success — everything already delivered via batches). Deterministic `batch_id`/`idempotency_key` (`batching.py`) make a retried batch submission, or a full job replay, a safe replay producing identical IDs, never a duplicate. | `uv run pytest tests/unit/communication_processing/test_communication_worker_orchestration.py tests/unit/communication_processing/test_communication_worker_batches.py tests/unit/communication_processing/test_communication_provenance.py` | Passing | See `docs/qa/test-results.md` |
| COMM-IDENTIFIER-001 | Sarthak (Phase 3) | `social/identifiers.py::extract_mentioned_identifiers` extracts `phone_number`/`email_address`/`url`/`username_or_handle` from chat message text via fixed regexes, never confusing an email's local part for a handle, trimming trailing sentence punctuation from URL matches; every extracted mention reuses its parent message's exact locator; multiple identical matches within one message get distinct, stable `observation_id`s via `worker._observations_for`'s positional discriminator despite sharing that locator. | `uv run pytest tests/unit/communication_processing/test_social_identifiers.py tests/unit/communication_processing/test_communication_worker_orchestration.py -k mentioned_identifiers` | Passing | See `docs/qa/test-results.md` |
| COMM-TRANSLIT-WIRING-001 | Sarthak (Phase 3) | `chat_message_to_mention` attaches one transliteration candidate per whitespace-separated word in a message's `sender` field (never treating a multi-word name as one unmappable blob), each carrying `original_text`/`script`/`candidates`/`method`/`method_version`/`category`, always alongside the unchanged raw `sender` value; an oversized sender field is truncated (`MAX_HANDLE_LENGTH`), never fails the message. | `uv run pytest tests/unit/communication_processing/test_social_common.py` | Passing | See `docs/qa/test-results.md` |
| COMM-TIMEZONE-DEFAULT-001 | Sarthak (Phase 3) | A naive chat timestamp with no explicit offset/`Z`/epoch signal (WhatsApp always, Telegram when `date_unixtime` is absent, generic-JSON's naive ISO strings) resolves to UTC via `Settings.communication_default_timezone` (default `Asia/Kolkata`), recording the resolved zone in `timestamp_source_timezone`; an explicit signal still resolves directly with `timestamp_source_timezone=None`; a genuinely unparseable string leaves both `None`. | `uv run pytest tests/unit/communication_processing/test_whatsapp.py tests/unit/communication_processing/test_telegram.py tests/unit/communication_processing/test_json_records.py -k timezone` | Passing | See `docs/qa/test-results.md` |
| COMM-ASR-ADAPTER-001 | Sarthak (Phase 3) | `AsrAdapter`/`DiarizationAdapter` are genuinely implementable `Protocol`s; the sole production adapter of each (`UnavailableAsrAdapter`/`UnavailableDiarizationAdapter`) always reports `state=UNAVAILABLE` and always raises rather than fabricate a result; the explicit fixture-only adapters (`tests/fixtures/communication_processing/`) are clearly labeled (`model_name` contains `"fixture"`) and never imported by `app/`; every `diarization_speaker_turn` observation carries `segment_source="metadata_supplied"` (no adapter in this phase ever produces `"model_derived"`). | `uv run pytest tests/unit/communication_processing/test_asr_adapter.py tests/unit/communication_processing/test_diarization_adapter.py tests/unit/communication_processing/test_diarization_import.py -k segment_source` | Passing | See `docs/qa/test-results.md` |
| MEDIA-CLASSIFY-001 | Gaurav | `source.classify_media` accepts exactly seven media types (`video/mp4`, `video/quicktime`, `video/x-msvideo`, `video/x-matroska`, `image/jpeg`, `image/png`, `image/webp` — matroska added in the Phase 2 completion worker build), cross-checked against filename extension (never either signal alone); `worker._check_source_type` rejects an `EvidenceRecordV1.source_type` inconsistent with the classified modality. | `uv run pytest tests/unit/media_processing/test_source.py tests/unit/media_processing/test_media_worker.py -k source_type` | Passing | See `docs/qa/test-results.md` |
| VIDEO-PROBE-001 | Gaurav | `video/probe.py`'s `ffprobe` adapter extracts container format, duration, dimensions, frame rate, frame count, codec, audio presence, and rotation defensively — a missing/malformed field never crashes the probe (defaults to `None`), but a missing duration/width/height is a hard `media_probe_failed`; a missing `ffprobe` binary fails as `ffprobe_unavailable`. | `uv run pytest tests/unit/media_processing/test_probe.py tests/integration/media_processing/test_video_pipeline.py -k probe` | Passing | See `docs/qa/test-results.md` |
| VIDEO-SAMPLING-001 | Gaurav | `uniform_interval`/`fixed_fps`/`explicit_timestamps` sampling strategies are deterministic (identical metadata + config always yields identical, deduplicated, ascending timestamps); exceeding `SamplingRequest.max_frames` is a hard `sampling_limit_exceeded`, never a silent truncation. | `uv run pytest tests/unit/media_processing/test_sampling.py` | Passing | See `docs/qa/test-results.md` |
| FRAME-PROVENANCE-001 | Gaurav | Every video `object_detection`/`text_region_detection`/`ocr_text_mention` observation carries exact `frame_number` (only when `frame_count`+`frame_rate` are both reliably known, else `None` rather than guessed), `time_start_ms`, `time_end_ms`, and `bbox_xyxy_normalized`; repeated processing of the same video/sampling config produces identical observation IDs. | `uv run pytest tests/unit/media_processing/test_frames.py tests/unit/media_processing/test_media_provenance.py tests/integration/media_processing/test_video_pipeline.py -k provenance` | Passing | See `docs/qa/test-results.md` |
| IMAGE-DECODE-001 | Gaurav | `image/decoder.py` decodes JPEG/PNG/WebP via Pillow and extracts width/height/format/color-mode/orientation; a decompression-bomb-like image is rejected via `media_limit_exceeded` *before* pixel decode (dimensions read from the lazily-opened header only); unidentifiable/truncated data fails as `media_decode_failed`. | `uv run pytest tests/unit/media_processing/test_decoder.py` | Passing | See `docs/qa/test-results.md` |
| BBOX-NORMALISE-001 | Gaurav | `image/geometry.to_normalized` converts pixel-space boxes to `[0,1]`-normalized `bbox_xyxy_normalized`, rejecting (never clamping) non-finite values, non-positive image dimensions, and degenerate/inverted/out-of-range geometry. | `uv run pytest tests/unit/media_processing/test_geometry.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-WORKER-001 | Gaurav | `process_job` requires an explicit `detector`/`tracker`/`ocr` — `media_detection_v1` without a `detector` fails safely as `analysis_not_configured` rather than silently falling back to a fake; a malformed detection (out-of-range confidence or invalid bbox) is skipped, not fatal to the job; every emitted observation validates as `ObservationV1` and every result validates as `WorkerResultV1`. | `uv run pytest tests/unit/media_processing/test_media_worker.py tests/integration/media_processing/test_video_pipeline.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-ISOLATION-001 | Gaurav | `process_job` never imports a PostgreSQL/Neo4j/Redis/MinIO/queue client or `app.modules.graph`/`app.modules.structured_processing`/`app.modules.access_control` (static AST check across every file in the module), never imports `EntityV1`/`EventV1`, and never leaks a local filesystem path, raw subprocess output, or raw frame/image bytes through an error message. | `uv run pytest tests/unit/media_processing/test_media_safety.py` | Passing | See `docs/qa/test-results.md` |
| GPU-CAPABILITY-001 | Gaurav | `capability.detect_capability()` never raises regardless of GPU/`nvidia-smi` absence, a driver-less `nvidia-smi`, a timeout, or unparseable output — always returns a safe `cpu_only=True` report in those cases; `ffmpeg`/`ffprobe` availability is reported independently and correctly. | `uv run pytest tests/unit/media_processing/test_capability.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-PERFORMANCE-001 | Gaurav | `performance.Stopwatch` records non-negative, accumulating per-stage timings and frame counters, never a raw frame/image payload; `worker.process_job`'s optional `stopwatch` keyword is a pure side-channel that never changes the returned `WorkerResultV1`; no fixed throughput figure is ever asserted, only measured values. | `uv run pytest tests/unit/media_processing/test_performance.py tests/unit/media_processing/test_media_worker.py -k stopwatch` | Passing | See `docs/qa/test-results.md` |
| MEDIA-ROUTING-001 | Nipun (originally Gaurav) | `image`/`video` upload routing (jpeg/png, mp4/quicktime/matroska) to `media_detection_v1` (Phase 2 closeout re-route from `media_metadata_v1` — see `docs/architecture/phase-2-decisions.md`); cross-MIME rejection before storage write; a client-supplied `parser_profile` is never honored; idempotent replay; cross-case isolation. | `uv run pytest tests/unit/evidence_lifecycle/test_media_routing.py tests/unit/media_processing/test_source.py -k matroska` | Passing | See `docs/qa/test-results.md` |
| MEDIA-WORKER-CLIENT-001 | Gaurav / Nipun | `client.WorkerApiClient` speaks Nipun's exact wire format (claim/submit_result/fetch_input/renew request bodies, header names, status-code handling) against a fake transport; a worker token/claim token never appears in a log line at any level. | `uv run pytest tests/unit/media_processing/test_media_worker_client.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-WORKER-ORCHESTRATION-001 | Gaurav / Nipun | `run_once` claim → resolve → SHA-256-verify → `process_job` → submit sequencing; a SHA-256 mismatch fails safely (`evidence_integrity_mismatch`) *before* any decode is attempted; an unreachable input endpoint defers (`input_resolution_unavailable`) rather than crashing; a "no job available" claim is a normal, non-error outcome; `SUPPORTED_PROCESSORS` tries `media_detection_v1` then `media_metadata_v1` live; `_effective_processors` falls back to metadata-only when no detector loaded. | `uv run pytest tests/unit/media_processing/test_media_worker_orchestration.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-GRAPH-LIVE-001 | Gaurav / Nipun | The complete real path for both image and video evidence: upload → server-selected `media_detection_v1` → real worker `--once` claim/input/SHA-verify/result (real detector/OCR when locally bootstrapped, metadata-only otherwise) → persisted `ObservationV1` → durable `graph_projection_jobs` row (exactly one for the metadata observation) → real `graph.worker --once` projection (run twice, confirming no duplicate node/relationship on the second run) → real Neo4j query and the real `GET /api/v1/cases/{case_id}/graph/observations` endpoint, both confirmed free of `object_uri`/credential/raw-media leakage. | `uv run pytest tests/integration/media_processing/test_media_worker_live.py` (self-skips without live infra); also verified via a real `subprocess`-invoked `--once` CLI, not only this in-process pytest call | Passing | See `docs/qa/test-results.md` |
| MEDIA-DETECTOR-001 | Nipun | `OnnxObjectDetector`: missing/checksum-mismatched model asset fails clearly (`ModelAssetError`) before ever reaching onnxruntime; `device="cuda"` without a real CUDA execution provider fails clearly rather than silently running on CPU; a committed synthetic frame produces well-formed, deterministic, native-`float` `ObjectDetection`s (label/confidence/normalized-safe pixel box) when the model asset is locally bootstrapped (self-skips real-detection scenarios otherwise; never downloads the model itself). | `uv run pytest tests/unit/media_processing/test_onnx_detector.py` | Passing (4 failure-mode always; 4 real-detection self-skip without a bootstrapped model) | See `docs/qa/test-results.md` |
| MEDIA-OCR-001 | Nipun | `TesseractTextRecognizer`: missing binary/language pack fails clearly (`OcrRuntimeError`); real rendered text is recognized with correct text/confidence/bounding box (self-skips without a suitable local TrueType font); a confidence floor filters low-confidence regions; a blank image finds no text; recognized text is never logged. | `uv run pytest tests/unit/media_processing/test_tesseract_ocr.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-OCR-ADAPTER-001 | Gaurav | `ImageOcrAdapter` runs genuine local Tesseract against labelled synthetic PNG and JPEG when available; returns ordered line-level original-space boxes, valid normalized geometry, bounded OCR-quality confidence, and safe engine/config metadata. Fixture output remains explicit and deterministic; missing binary/language and corrupt raster input fail safely. | `uv run pytest tests/unit/media_processing/test_ocr_adapter.py` | Passing locally (`tesseract 5.5.2`) | See `docs/qa/test-results.md` |
| MEDIA-OCR-BATCHING-001 | Gaurav | Bounded image/video OCR batches carry only their own output IDs and safe provenance. The real `run_once` closure is exercised with a fake worker client: SHA verification → OCR batch(es) → media batch → empty terminal result; video sequences are global zero-based, frame locators are complete, and a final video progress batch is marked final. Batch failure yields one safe retryable terminal failure. | `uv run pytest tests/unit/media_processing/test_ocr_batching.py tests/unit/media_processing/test_media_worker_orchestration.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-OCR-LIVE-001 | Gaurav | Against a live Compose stack: a real labelled PNG/JPEG image, and separately a real labelled synthetic MP4, are each uploaded → claimed via the real `run_once` → OCR'd by the genuine local `ImageOcrAdapter` (never `FixtureOcrAdapter`) → submitted as bounded micro-batches with a terminal result carrying `observations=[]` → durably persisted with exactly one `graph_projection_jobs` row per observation → the real graph projector run twice (idempotent, no duplicate work on the second run) → confirmed via both a direct Neo4j query and the real `GET /api/v1/cases/{case_id}/graph/observations` endpoint. The video case additionally asserts every OCR observation carries a `frame_number` + valid `time_start_ms`/`time_end_ms` locator and that the recognized text spans real, distinct sampled frames. A separate test proves identical-batch replay is idempotent (no duplicate rows/outbox jobs); another confirms no response ever leaks an object URI, claim token, or worker credential. Self-skips (never fabricates a pass) when the live API/PostgreSQL/Neo4j/MinIO, `ffmpeg`/`ffprobe` (video only), a real TrueType font, or the local Tesseract binary are unavailable. | `uv run pytest tests/integration/media_processing/test_media_worker_live.py -k ocr_batch -v` | Self-skips (unverified) whenever Docker/live infra is unavailable in a given environment — see `docs/qa/test-results.md` for the exact run and status | See `docs/qa/test-results.md` |
| MEDIA-TRACKER-001 | Nipun | `IoUTracker` (the real, promoted `analysis/fake_tracker.py` algorithm): deterministic track IDs; a label change never bridges a track; overlapping same-time detections are preserved, never merged; a gap ends a track rather than bridging it; the configured IoU threshold is genuinely load-bearing; `TrackSegment` carries no cross-evidence/case identity field. | `uv run pytest tests/unit/media_processing/test_iou_tracker.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-WHOLE-FRAME-OCR-001 | Nipun | The independent whole-frame `recognize_regions` OCR pass (not gated behind the detector's own `text_region` label, since a COCO-class detector has none) emits `ocr_text_mention` observations for both the image and video paths, in addition to the pre-existing detector-gated crop pathway. | `uv run pytest tests/unit/media_processing/test_media_worker.py -k text_region` | Passing | See `docs/qa/test-results.md` |
| MEDIA-WORKER-LOOP-001 | Nipun | `run_loop`: stops immediately if shutdown is already requested; claims no further job once shutdown is requested mid-loop; bounded exponential backoff on repeated `WorkerApiError`, stopping at `max_consecutive_failures`; a success resets the failure counter; claimed jobs loop again immediately without an idle sleep. `_lease_heartbeat`: renews periodically while wrapped work runs, stops renewing once the block exits, swallows renewal failures, joins its thread before returning, never logs the claim token. | `uv run pytest tests/unit/media_processing/test_media_worker_loop.py` | Passing | See `docs/qa/test-results.md` |
| MEDIA-LEASE-RENEWAL-001 | Nipun (extended: Aditya, Phase 3) | `POST /api/v1/internal/worker-jobs/{job_id}/renew`: a valid renewal extends the lease and leaks no claim token/object URI; missing/wrong/different-worker claim tokens, a not-running job, and an already-expired lease are all rejected via the same generic `401` (audited as `worker_job_access_denied`); `service.renew_claim`'s verification ordering (unknown/token-mismatch/identity-mismatch/not-running/lease-expired) matches `submit_result`/`get_claimed_evidence_input` exactly. A successful renewal is now also audited (`worker_job_lease_renewed`, `record_audit_event_safely`, safe `case_id`/`job_id`/`evidence_id`/`attempt` metadata only). | `uv run pytest tests/unit/evidence_lifecycle/test_worker_lease_renewal.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-LEASE-CEILING-001 | Aditya | A renewal is capped, via `LEAST(...)` evaluated inside the same atomic `UPDATE ... RETURNING`, at `claimed_at + Settings.worker_lease_max_seconds` (default 3600s) for that attempt — never granted past the ceiling regardless of how many times or how frequently `/renew` is called; once real time passes the ceiling, the (no-longer-advancing) lease falls behind `now` and renewal is rejected exactly like any other expired lease, with no separate rejection path needed. `Settings` fails fast at construction if `worker_lease_max_seconds < worker_lease_seconds`. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_lease_renewal.py -k absolute_maximum` | Passing | See `docs/qa/test-results.md` |
| WORKER-SUCCESS-AUDIT-001 | Aditya | Every worker-lifecycle *success* is now audited, not only denials: `worker_job_claimed`/`worker_job_reclaimed` (split by whether the claim was a job's first or a lease-expiry reclaim), `worker_job_completed`/`worker_job_failed` (split from a single generic result-accepted event by the submitted `WorkerResultV1.status`), and `worker_job_retry_exhausted` — each with safe `worker_id`/`job_id`/`evidence_id`/`case_id`/`attempt` fields only, never a claim token or worker token. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_identity_api.py -k "claimed_audit_event or reclaimed_audit_event" tests/unit/evidence_lifecycle/test_worker_internal_api.py -k "completed_audit_event or failed_audit_event"` | Passing | See `docs/qa/test-results.md` |
| WORKER-RETRY-LEASE-LIVE-001 | Aditya | Against a live Compose stack, driving `EvidenceLifecycleService` directly with fully controllable `UploadContext.now` (no raw-SQL time simulation needed): a real renewal is capped at the absolute ceiling and later rejected once real time passes it; a real lease-expiry reclaim invalidates the old claim token (rejected `token_mismatch`) while the new claimant's token works; real retry exhaustion (`worker_job_max_attempts=2`) durably transitions a job to `failed` with `error_code="retry_exhausted"`; two genuinely concurrent (`asyncio.gather`) terminal `/result` submissions for the same job resolve to exactly one accepted result and one safe conflict, never a contradictory or duplicated persisted state. | `uv run pytest tests/integration/evidence_lifecycle/test_worker_retry_and_lease_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| GRAPH-WORKER-LOOP-001 | Nipun | `graph.worker.run_loop` (with `run_batch` monkeypatched -- no real PostgreSQL/Neo4j needed, `create_engine`/`create_driver` build lazy clients): stops on shutdown, claims no further batch once requested mid-loop, bounded exponential backoff + `max_consecutive_failures` cutoff, failure-counter reset on success, accumulates `total_claimed`/`total_succeeded` across batches. | `uv run pytest tests/unit/graph/test_worker_loop.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-LEASE-RENEWAL-001 | Nipun | `run_batch`'s `renew_interval_seconds`: with it unset (the `--once` default), never renews any lease; with it set, renews only *not-yet-attempted* jobs' leases once enough injected `monotonic()` time has elapsed, never an already-completed job's. | `uv run pytest tests/unit/graph/test_projector.py -k renew` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-UPLOAD-001 | Nipun | A valid multipart upload creates exactly one `evidence_records` row, one private MinIO object write, and one `worker_jobs` row in a single transaction; SHA-256 matches the uploaded bytes exactly; a large upload is hashed via bounded 1 MiB chunks, never buffered in full. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-KEY-SAFETY-001 | Nipun | The private object key (`cases/{case_id}/evidence/{evidence_id}/original`) is a pure function of two server-generated UUIDs; the caller-supplied filename never appears in it regardless of path-traversal or shell-metacharacter content, and is preserved only as display metadata. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py::test_object_key_never_contains_the_caller_supplied_filename tests/unit/evidence_lifecycle/test_object_storage.py` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-VALIDATION-001 | Nipun | An unsupported `source_type`/`content_type` combination, an empty (zero-byte) upload, a missing filename, and an oversized upload (exceeding `MAX_EVIDENCE_BYTES`) are all rejected safely (`422`/`413`) before any object-storage write or database row is created. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py -k "unsupported or empty or missing_filename or oversized" tests/unit/evidence_lifecycle/test_evidence_api.py -k "unsupported or empty"` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-IDEMPOTENCY-001 | Nipun | A retried upload with the same `Idempotency-Key` and identical content returns the original evidence/job (`200`, no duplicate storage write or job row, no duplicate publish); the same key reused for different content is rejected `409`; a true concurrent race is resolved safely via the database's own partial unique constraint, with the loser's orphaned object cleaned up. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py -k idempotency tests/unit/evidence_lifecycle/test_evidence_api.py -k idempotency` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-FAILURE-RECOVERY-001 | Nipun | A simulated object-storage failure leaves no evidence/job row and publishes nothing; a simulated database failure after a successful storage write deletes the orphaned object and re-raises safely; a simulated job-publication (Redis) failure does not fail the upload and leaves a durable, queryable `dispatched_at IS NULL` row rather than losing the job. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py -k "storage_failure or db_persistence or dispatch"` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-AUTHZ-001 | Nipun | Upload/list/detail all require an active case membership via `require_case_action`/`require_evidence_read`; a caller without any membership on the target case is denied `403` on every one of upload/list/detail; a member of case A can never read case B's evidence, even by guessing a valid `evidence_id`. | `uv run pytest tests/unit/evidence_lifecycle/test_evidence_api.py -k "without_case_membership or case_a_member"` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-SAFE-RESPONSE-001 | Nipun | Evidence/job API responses never contain `object_uri`, a MinIO/Postgres credential, or the substring `"minio"`/`"secret"`/`"password"`; a storage failure surfaces as the generic safe `500` envelope, never the underlying exception's message. | `uv run pytest tests/unit/evidence_lifecycle/test_evidence_api.py -k "never_exposes or storage_failure"` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-CONTRACT-001 | Nipun | Every `WorkerJobV1` this module constructs round-trips through the frozen contract's own validators (idempotency-key pattern, required fields) without modification; the routed `processor_name`/`processor_version` matches the documented registry for the declared `source_type`. | `uv run pytest tests/unit/evidence_lifecycle/test_upload_service.py::test_generated_job_validates_against_the_frozen_worker_job_contract` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-ISOLATION-001 | Nipun | `app/modules/evidence_lifecycle/` never imports `app.modules.graph`/`structured_processing`/`communication_processing`/`media_processing` or any OCR/media/vector/ML library (static AST check), never imports/constructs `EntityV1`/`EventV1`/`ObservationV1`/`WorkerResultV1`/`WorkerProgressV1`, and never imports `neo4j`. | `uv run pytest tests/security/evidence_lifecycle/test_evidence_lifecycle_boundaries.py` | Passing | See `docs/qa/test-results.md` |
| EVIDENCE-MIGRATION-001 | Nipun | `f2086e1e89f6_evidence_lifecycle_foundation` applies cleanly on top of the existing chain against a fresh live PostgreSQL; `evidence_records.uploaded_by`/`case_id` and `worker_jobs.case_id`/`evidence_id` foreign keys are enforced; the partial unique index on `(case_id, upload_idempotency_key)` and the unique index on `worker_jobs.idempotency_key` are enforced by the real database, not just application logic. | `uv run pytest tests/integration/evidence_lifecycle -v` | Passing (live) | See `docs/qa/test-results.md` |
| EVIDENCE-LIVE-001 | Nipun | Against a live Compose stack: a real multipart upload through the running API creates the expected PostgreSQL rows and a real private MinIO object with byte-identical content; `/healthz`/`/readyz`/`/api/v1/meta/contracts` remain correct with the evidence-lifecycle router and startup bucket-init hook wired in. | `uv run pytest tests/integration/evidence_lifecycle -v` plus a manual `curl` smoke test (see `docs/qa/test-results.md`) | Passing (live) | See `docs/qa/test-results.md` |
| WORKER-CLAIM-001 | Nipun (extended: Aditya, Phase 3) | `claim_job` atomically claims exactly one eligible `queued` (or expired-lease `running`) job matching the declared `processor_name`/`processor_version` via `FOR UPDATE SKIP LOCKED`; two concurrent claims for one job never both succeed; a processor/version mismatch or no eligible job returns a safe empty result, never an error; an expired lease is reclaimed with `attempt` incremented (never on a first claim), and a cancelled/terminal job is never auto-retried. A job's first-ever claim is always allowed regardless of `max_attempts`; only a reclaim is gated on `attempt < max_attempts`. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_claim.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-RETRY-LIMIT-001 | Aditya | A job whose lease keeps expiring is reclaimed only while `attempt < max_attempts` (`Settings.worker_job_max_attempts`, mirroring `graph_projection_jobs.max_attempts`'s established policy); once exhausted, the next `claim_job` call for *any* processor (opportunistic, unscoped sweep) transitions it durably to `failed` via the existing `submit_result` write path with a synthetic terminal `WorkerResultV1` (`error.code="retry_exhausted"`) — a real `worker_results` row is always created, never a status-only transition; a concurrent double-sweep is self-healing via the pre-existing `worker_results.job_id` unique constraint. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_claim.py -k retry_exhaustion` | Passing | See `docs/qa/test-results.md` |
| OCR-TEXT-UTILITY-001 | Jasraj | Pure OCR-fragment post-processing preserves raw text, supplies line-preserving display/matching variants, deterministic profile hash/transformation sequence, raw span/token/locator/lineage provenance, and only conservative unresolved identifier candidates. Control/surrogate input is safe; no raw text, URI, credential, media bytes, graph write, entity merge, or OCR correction is produced. | `uv run pytest tests/unit/extracted_text/test_ocr_postprocessing.py` | Passing | Synthetic fixtures only; see `docs/qa/test-results.md` |
| MEDIA-PHASE4-PLAN-001 | Gaurav | Rapid/deep profile hashes, immutable Nipun manifest planning, bounded stable adaptive samples, explainable frame-difference signal, and candidate-only safe visual metadata are deterministic. | `uv run pytest tests/unit/media_processing/test_phase4_planning.py` | Passing | Synthetic metadata/arrays only; no GPU/model/media/LAN |
| WORKER-RESULT-SUBMIT-001 | Nipun | `submit_result` validates the claim token (missing/wrong/expired all rejected uniformly), the submitted `WorkerResultV1`'s `job_id`/`case_id`/`evidence_id` and every `ObservationV1`'s `case_id`/`evidence_id` against the claimed job, and that `status` is a terminal outcome (`succeeded`/`failed`/`deferred`/`cancelled`) — before persisting result + observations + the job's terminal status atomically in one transaction. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_result.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-RESULT-IDEMPOTENCY-001 | Nipun | An exact-payload resubmission for an already-terminal job returns the original accepted outcome (`result_id`, `observation_ids` unchanged, no duplicate row); a different payload for an already-terminal job is a safe `409`; a true concurrent race (two submissions racing the `worker_results.job_id` unique constraint) resolves via the same re-read-and-compare pattern `create_evidence_with_job`'s idempotency race already established; a failed persistence transaction leaves the job non-terminal with zero result/observation rows. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_result.py -k "duplicate or conflict or persistence"` | Passing | See `docs/qa/test-results.md` |
| WORKER-INTERNAL-API-001 | Nipun (auth updated: Aditya, Phase 2.4) | `/api/v1/internal/worker-jobs/{claim,{job_id}/result}` are gated by `require_worker_principal` (fails closed `503` when the deployment's required `WORKER_CREDENTIAL_PEPPER` is missing, `401` on a missing/unknown/revoked worker credential — see `AUTH-WORKER-IDENTITY-001` for the full per-worker-credential test scope); claim returns the full `WorkerJobV1` plus a one-time claim token (never persisted raw) or a safe empty "no work" response; result submission requires a valid `X-Claim-Token` header and never leaks an object URI, credential, or the claim token itself in any response. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_internal_api.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-JOB-STATUS-001 | Nipun | The existing user-facing `GET /api/v1/cases/{case_id}/jobs/{job_id}` now also safely exposes `claimed_at`, `completed_at`, and `observation_count` once a result exists, while still never exposing a claim token, `claim_token_hash`, or `object_uri`; remains case-scoped exactly as before. | `uv run pytest tests/unit/evidence_lifecycle/test_evidence_api.py -k completed_job_status` | Passing | See `docs/qa/test-results.md` |
| WORKER-LIVE-001 | Nipun | Against a live Compose stack: a real claim via `FOR UPDATE SKIP LOCKED` against live PostgreSQL, followed by a real synthetic `WorkerResultV1` (one `ObservationV1`) submission, produces a durable terminal `worker_jobs` row and a durable `worker_observations` row; a processor/version mismatch finds nothing to claim; a stale claim token against an already-terminal job with different content is a safe conflict. | `uv run pytest tests/integration/evidence_lifecycle/test_worker_lifecycle_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| SP-WORKER-CLIENT-001 | Jasraj | `WorkerApiClient` speaks Nipun's internal-API wire format exactly against an `httpx.MockTransport`: claim success/no-work, claim auth failure (`401`/`503`), submit success/conflict(`409`)/validation-error(`422`)/wrong-token(`401`), and `fetch_input` success (parsing `Content-Disposition`/`X-TraceX-Evidence-SHA256`), an oversized response rejected before parsing, and the defensive `404` -> `InputResolutionUnavailableError` fallback; the shared secret and claim token never appear in a captured log line. | `uv run pytest tests/unit/structured_processing/test_worker_client.py` | Passing | See `docs/qa/test-results.md` |
| SP-WORKER-ORCHESTRATION-001 | Jasraj | `run_once` claims through each `SUPPORTED_PROCESSORS` entry in turn until one yields a job (or exits `claimed=False` cleanly when none do); on a real claim it resolves input, verifies the resolved bytes' SHA-256 against the API-reported value (a mismatch submits `FAILED`/`evidence_integrity_mismatch` before ever calling `process_job`), calls the unchanged `process_job`, and submits the result with the correct claim token threaded through; when the injected resolver raises `InputResolutionUnavailableError`, it submits a `DEFERRED` result with checkpoint `input_resolution_unavailable` instead of crashing or fabricating success; a `submit_result` failure propagates without ever including the claim token in its message; `main`'s `--once` CLI wiring returns `1` when `WORKER_TOKEN` is unconfigured and `0` on a clean no-job exit. | `uv run pytest tests/unit/structured_processing/test_worker_orchestration.py` | Passing | See `docs/qa/test-results.md` |
| SP-WORKER-LIVE-001 | Jasraj | Against a live Compose stack: the real `WorkerApiClient` reaches the real running internal API and gets a genuine "no work" claim response back when nothing is seeded, and a genuine `401` (`WorkerAuthenticationError`) from `fetch_input` against a `job_id` with no live claim. A second test proves the *complete* path for real (enabled by Phase 2.2's now-real input-stream endpoint): a real case/user/evidence upload followed by a real `run_once()` completes claim -> stream -> parse -> submit, producing a genuine `SUCCEEDED` result with real observations, confirmed independently via the job-status API. Self-skips entirely (never fabricates success) if there's no `.env`, the live server/PostgreSQL/MinIO aren't reachable, or `WORKER_TOKEN` isn't configured. | `uv run pytest tests/integration/structured_processing/test_worker_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| WORKER-INPUT-STREAM-001 | Nipun | `GET /api/v1/internal/worker-jobs/{job_id}/input` requires both `require_worker_principal` and the exact claim token for a currently-`running`, unexpired-lease job — a missing/wrong/oversized/malformed token, a different job's token, a `queued`/terminal job, and an expired lease are all rejected `401` uniformly; a valid request streams the exact stored bytes with safe headers only (no object key, bucket, MinIO endpoint, or claim token ever present in headers/logs); a missing backing object fails safely without leaking storage details. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_input_api.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-INPUT-STREAM-002 | Nipun | `ObjectStorage.open_stream`/`_stream_response` pull chunks lazily from the underlying object (never eagerly reading it all before the caller asks), and close/release the underlying storage response exactly once, in a `finally`, regardless of how much of the stream was actually consumed. | `uv run pytest tests/unit/evidence_lifecycle/test_object_storage.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-INPUT-STREAM-LIVE-001 | Nipun | Against a live Compose stack: a real case/user/evidence upload followed by a real `structured_processing` worker run completes the full `claim -> stream evidence via GET .../input -> parse -> submit WorkerResultV1` path, producing a durable `SUCCEEDED` result with real, persisted `ObservationV1` rows — not a `DEFERRED` fallback. Parametrized over `document`/`fir_report_text_v1`, `structured_tabular`/`generic_tabular_v1`, and `structured_json`/`generic_json_v1` (Phase 2.3). Same underlying path also confirmed via a one-off manual `--once` CLI smoke test. | `uv run pytest tests/integration/structured_processing/test_worker_live.py::test_full_claim_stream_parse_submit_live_pipeline -v` | Passing (live) | See `docs/qa/test-results.md` |
| STRUCTURED-ROUTING-001 | Nipun | `structured_tabular` (`text/csv`/XLSX) and `structured_json` (`application/json`) route deterministically to `generic_tabular_v1`/`generic_json_v1` respectively; a MIME type outside a source type's own allowlist (including the *other* new type's MIME) is rejected `422` before any storage write; `source_type=other` remains rejected; existing `document`/`cdr`/`financial` routing is unaffected. | `uv run pytest tests/unit/evidence_lifecycle/test_structured_routing.py tests/unit/evidence_lifecycle/test_upload_service.py -k other` | Passing | See `docs/qa/test-results.md` |
| STRUCTURED-ROUTING-002 | Nipun | A client-supplied `parser_profile` form value is never honored, for `structured_tabular`/`structured_json` or any other source type — the persisted `EvidenceRecordV1.parser_profile` and the created job's `processor_name` are always the server-computed canonical route, independent of what the client sent. | `uv run pytest tests/unit/evidence_lifecycle/test_structured_routing.py::test_client_cannot_override_routed_processor_or_parser_profile` | Passing | See `docs/qa/test-results.md` |
| STRUCTURED-ROUTING-003 | Nipun | Idempotent replay (`Idempotency-Key`) and cross-case isolation both hold for `structured_tabular`/`structured_json` evidence exactly as for every other source type — a retried identical upload returns the original evidence/job without a duplicate write, and a case-A member can never read case-B's structured evidence. | `uv run pytest tests/unit/evidence_lifecycle/test_structured_routing.py -k "idempotent or cross_case or case_a"` | Passing | See `docs/qa/test-results.md` |
| AUTH-WORKER-IDENTITY-001 | Aditya | The `worker_credentials` CLI (`create`/`rotate`/`revoke`/`list`) issues `secrets.token_urlsafe(32)` tokens shown in plaintext exactly once and stores only an HMAC-SHA256(pepper)-or-SHA-256 digest; `list` never prints a plaintext token or digest; a revoked or never-issued credential is rejected `401` immediately; a missing `WORKER_CREDENTIAL_PEPPER` fails closed `503` only in production, never silently falling back to an unpeppered/shared secret. | `uv run pytest tests/unit/access_control/test_worker_credentials.py tests/unit/evidence_lifecycle/test_worker_identity_api.py -k "authenticates or missing_authorization or malformed_or_blank or unknown_token or revoked"` | Passing | See `docs/qa/test-results.md` |
| WORKER-PROCESSOR-SCOPE-001 | Aditya | `/claim` accepts a worker only for a `processor_name` present in that worker's own `allowed_processor_names`; a worker credential with no scope for the requested processor is denied and the denial is audited (`worker_processor_scope_denied`) without ever leaking another worker's scope or the claim outcome. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_identity_api.py -k "worker_can_claim_an_allowed_processor or worker_with_no_scope_for_a_processor"` | Passing | See `docs/qa/test-results.md` |
| WORKER-JOB-OWNERSHIP-001 | Aditya | A successful `/claim` persists `claimed_by_worker_id` for the authenticated worker; a second worker holding a stolen/guessed claim token can never fetch input or submit a result for a job it doesn't own; worker-identity is verified unconditionally *before* the terminal-result replay/idempotency branch runs; a legitimate lease-expiry reclaim transfers ownership to the new claiming worker. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_identity_service.py` | Passing | See `docs/qa/test-results.md` |
| PHASE5B-SOURCE-SIGNALS-001 | Jasraj | CDR and finance records require both role-labelled parties and source-backed timestamps; CSV/XLSX/JSON aliases, locators, case/evidence/extractor provenance, safe validation metadata, exact decimal/currency handling, invalid time/duration/amount rejection, and deterministic outcomes are covered. OCR region/page/span/bbox/confidence validation retains only safe engine metadata. | `uv run pytest tests/unit/structured_processing/test_phase5_signal_validation.py tests/unit/structured_processing/test_cdr.py tests/unit/structured_processing/test_finance.py` | Passing | Synthetic fixtures only; no direct graph writes or scoring. |
| WORKER-DENIAL-AUDIT-001 | Aditya | `case_access_denied`, `worker_authentication_denied`, `worker_processor_scope_denied`, and `worker_job_access_denied` are all recorded through the existing audit service (no parallel store) with only safe fields (request ID, actor type, user/worker ID when known, case/job ID when safe, action, reason code) — never a bearer token, claim token, object URI, storage detail, request body, or stack trace; an audit-write failure never turns a deny into a grant; `require_case_action`'s previously undocumented denial gap is now covered. | `uv run pytest tests/unit/access_control/test_case_access_audit.py tests/unit/evidence_lifecycle/test_worker_identity_api.py -k "authentication_denial_is_audited or job_access_denial_is_audited or no_audit_event_ever_contains"` | Passing | See `docs/qa/test-results.md` |
| GRAPH-ENTITY-MENTION-001 | Shreshtha | `project_observation_mentions` MERGEs each `ObservationV1.extracted_entities` entry as an evidence-local `EntityMention` (never an `EntityV1`, never merged with another mention); `mention_id` is deterministic from `(case_id, observation_id, ordinal, normalized text)` — stable across re-projection, distinct across case/observation/ordinal/text; defers safely when the parent observation isn't projected yet; a 0-mention observation is a no-op write. | `uv run pytest tests/unit/graph/test_projection.py -k mention` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROJECTION-OUTBOX-001 | Shreshtha | An accepted worker result durably enqueues one `graph_projection_jobs` row per observation, atomically with `worker_results`/`worker_observations`; a Neo4j connection failure leaves the job safely retryable (`queued`), never discarding the already-accepted canonical result; retries are bounded by `max_attempts`, after which the job is left durably `failed` for inspection, never silently dropped. | `uv run pytest tests/unit/evidence_lifecycle/test_worker_result.py -k projection_job tests/unit/graph/test_projector.py` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROJECTION-OUTBOX-LIVE-001 | Shreshtha | Against a live Compose stack: real `FOR UPDATE SKIP LOCKED` concurrency safety (two genuinely concurrent `claim_batch` calls never double-claim), real lease-expiry reclaim with `attempt` incremented, real retry exhaustion leaving a job `failed`, and the crash-recovery sweep for a `running` row whose lease expired with no attempts remaining. | `uv run pytest tests/integration/graph/test_outbox_repository_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| GRAPH-READ-API-001 | Shreshtha | `GET /api/v1/cases/{case_id}/graph/observations` is gated by `require_graph_read` (`CaseAction.GRAPH_READ`) exactly like every other case-scoped endpoint (403 + `case_access_denied` audit for a non-member, including cross-case); returns only case-scoped, allow-listed `Observation`/`EntityMention` data, bounded/paginated (`limit`/`offset`, capped at 200); never an object URI, raw evidence body, worker credential, claim token, or Cypher text — verified both behaviorally and by introspecting the response models' declared fields. | `uv run pytest tests/unit/graph/test_graph_api.py tests/security/graph/` | Passing | See `docs/qa/test-results.md` |
| GRAPH-PROJECTION-LIVE-001 | Shreshtha | Against a live Compose stack: a real upload → authenticated worker claim → secure evidence stream → worker result with a canonical `ObservationV1` (including an `extracted_entities` mention) → durable `graph_projection_jobs` row → `app.modules.graph.worker`'s projector run → both a real Neo4j query (`list_case_observations`) and the real case-scoped HTTP endpoint confirm the projected `Observation`/`EntityMention`. | `uv run pytest tests/integration/graph/test_full_pipeline_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| COMM-ROUTING-001 | Shreshtha | Five new, additive `SourceType` values (`audio_transcript`, `audio_diarization`, `whatsapp_chat`, `telegram_chat`, `instagram_chat`) route real uploads to `communication_processing`'s previously-unreachable `transcript_import_v1`/`diarization_import_v1`/`whatsapp_export_v1`/`telegram_export_v1`/`instagram_export_v1` profiles respectively; a MIME type outside a source type's own allowlist is rejected `422` before any storage write; existing `audio`/`chat` routing (`audio_metadata_v1`/`generic_social_json_v1`) is unaffected; a client-supplied `parser_profile` is never honored; idempotent replay and cross-case isolation both hold. | `uv run pytest tests/unit/evidence_lifecycle/test_communication_routing.py tests/contract/test_evidence.py -k communication_routing` | Passing | See `docs/qa/test-results.md` |
| COMM-ROUTING-E2E-001 | Shreshtha | For all five newly-routed source types, the complete real chain succeeds: upload → server-selected processor → real `communication_processing` worker `--once` claim/input/result → persisted `ObservationV1` → real graph projection via `graph.worker --once`, confirmed by both a direct Neo4j query and the `GET /api/v1/cases/{case_id}/graph/observations` endpoint. Also covers the worker's own internal `_validate_source_type` gate (a second, independent check beyond routing) recognizing all five new source types per-profile, not just by the old audio/chat grouping. | `uv run pytest tests/unit/communication_processing/ tests/integration/communication_processing/` (regression coverage); live verification is a manual script run against the full Docker Compose stack, not part of the default `uv run pytest` run | Passing | See `docs/qa/test-results.md`, "Origin/main integration and real end-to-end verification" section |
| OBSBATCH-CONTRACT-001 | Nipun | `ObservationBatchSubmissionV1`/`TransformationProvenanceV1`/`ObservationBatchProgressV1` parse valid document-page/span, scanned-bbox, CDR row/column, and finance sheet/row/cell observation batches and round-trip through JSON; reject an out-of-range confidence/invalid bbox/invalid time-range `ObservationV1`, a malformed/missing `batch_id`/`batch_sequence`/`submitted_at`/`idempotency_key`, a duplicate `observation_id` within one batch, an entirely empty batch (no observations/transformations/progress), a secret-shaped or overlong `safe_metadata` value, an invalid transformation ordering (`completed_at < started_at`)/duplicate ordinal/out-of-batch `output_observation_ids`/unknown `status`, and a negative/incoherent progress value. | `uv run pytest tests/contract/test_observation_batch.py` | Passing | See `docs/qa/test-results.md` |
| OBSBATCH-SERVICE-001 | Nipun | `EvidenceLifecycleService.submit_observation_batch` persists a valid multi-observation batch (receipt + observations + transformations + progress event) atomically; a batch outside the claimed job's case/evidence scope, or against a non-`running`/lease-expired job for a brand-new `batch_id`, persists nothing; an identical retry returns the original receipt with no duplicate rows; a changed payload under the same `(batch_id, idempotency_key)`, or the same `idempotency_key` reused for a different `batch_id`, is a safe conflict; an `observation_id` reused from a different already-accepted batch is a deterministic conflict, never a second row or second graph-projection job; a replay enqueues no additional projection job; a simulated persistence failure leaves zero orphaned rows; progress events remain ordered and reject regression within the same job `attempt` (a fresh reclaim's reset is allowed); transformation provenance is retrievable by case/evidence/job/batch linkage. | `uv run pytest tests/unit/evidence_lifecycle/test_observation_batch_submission.py` | Passing | See `docs/qa/test-results.md` |
| OBSBATCH-API-001 | Nipun | `POST /api/v1/internal/worker-jobs/{job_id}/observations` accepts a valid claimed worker's own batch; rejects a missing/wrong claim token, a different worker's valid token, an already-expired lease (for a brand-new batch), and an unknown job, all via the same generic `401`; rejects a path/body `job_id` mismatch and a cross-case/cross-evidence submission `422` with no persistence; a conflict response is a safe `409`; no response (`401`/`409`) ever contains `minio`/a secret/a password/SQL text/a stack trace/an object URI/the claim token. | `uv run pytest tests/unit/evidence_lifecycle/test_observation_batch_api.py` | Passing | See `docs/qa/test-results.md` |
| OBSBATCH-PROGRESS-001 | Nipun | The existing user-facing `GET /api/v1/cases/{case_id}/jobs/{job_id}` now also safely exposes `latest_progress` (the most recent accepted `worker_progress_events` row, if any) — case-scoped and authorized exactly as before, never leaking `object_uri`/`claim_token`; a job with no progress events, or no job at all for this case, behaves exactly as before (`null`/`404`). | `uv run pytest tests/unit/evidence_lifecycle/test_evidence_api.py -k progress` | Passing | See `docs/qa/test-results.md` |
| OBSBATCH-LIVE-001 | Nipun | Against a live Compose stack: a real document upload → claim → two real partial `POST .../observations` micro-batches durably persist `observation_batches`/`worker_observations`/`observation_transformations`/`worker_progress_events` rows and enqueue exactly one `graph_projection_jobs` row per observation (confirmed absent from Neo4j before projection, proving the ingestion endpoint itself never wrote to Neo4j); the real graph projector then projects both, a second run claims nothing further, and both a direct Neo4j query and the real `GET /api/v1/cases/{case_id}/graph/observations` endpoint confirm the projection. Separately: an identical batch replay against the live database creates no duplicate rows/outbox jobs; a partial batch followed by a terminal `POST .../result` (with no observations of its own) preserves the existing worker-job lifecycle and reports the correct combined `observation_count`. | `uv run pytest tests/integration/evidence_lifecycle/test_observation_batch_live.py -v` | Passing (live) | See `docs/qa/test-results.md` |
| OBSBATCH-MIGRATION-001 | Nipun | `c1c9c1c9d9f1_observation_batch_ingestion` applies cleanly on top of the existing chain against a fresh live PostgreSQL; `observation_batches`' `(job_id, batch_id)`/`(job_id, idempotency_key)` unique constraints, `worker_observations`' new `(result_id IS NULL) <> (observation_batch_id IS NULL)` check constraint, and every new foreign key are enforced by the real database, not just application logic. | `uv run alembic upgrade head` against a live PostgreSQL (exercised automatically by every test in `tests/integration/evidence_lifecycle/`) | Passing (live) | See `docs/qa/test-results.md` |
| DOC-PAGE-TRUST-001 | Jasraj | Every PDF page is classified `TEXT_TRUSTED`/`SCANNED_NO_TEXT`/`UNTRUSTWORTHY_TEXT_LAYER`/`CORRUPT` before any extraction runs; a page with *some* embedded text that fails the printable-ratio/token-quality checks is never blindly trusted; a page whose own text extraction raises is reported `CORRUPT` without aborting the rest of the document. | `uv run pytest tests/unit/structured_processing/test_pdf.py tests/unit/structured_processing/test_worker_document_batches.py -k corrupt` | Passing | See `docs/qa/test-results.md` |
| DOC-OCR-001 | Jasraj | A trusted embedded-text page never invokes OCR; a scanned/no-text or untrustworthy page runs real local Tesseract OCR (via `pypdfium2` rendering); a mixed document uses embedded-text extraction for trusted pages and OCR only for the pages that need it; every OCR-derived observation carries a valid page number and a normalized `[0,1]` bounding box. | `uv run pytest tests/unit/structured_processing/test_worker_document_batches.py` (self-skips OCR-dependent assertions only if `tesseract` is genuinely unavailable) | Passing | See `docs/qa/test-results.md` |
| DOC-OCR-PRECISION-001 | Jasraj | Real OCR + regex field-match precision/recall is measured on a labelled synthetic scanned-page fixture with known ground-truth identifiers (FIR number, phone number, amount) — an honest, reproducible measurement, not an assumed accuracy figure. | `uv run pytest tests/unit/structured_processing/test_ocr_field_match_precision.py -s` | Passing | See `docs/qa/test-results.md` for the actual measured precision/recall |
| DOC-NORMALIZATION-001 | Jasraj | Deterministic layout normalization (NFC, line-wrap dehyphenation, whitespace collapsing) maps every normalized span back to the exact original source span via a per-character offset map, including spans crossing a whitespace-collapse or dehyphenation boundary; identical input is always deterministic. | `uv run pytest tests/unit/structured_processing/test_normalization.py` | Passing | See `docs/qa/test-results.md` |
| DOC-NER-001 | Jasraj | The deterministic fallback NER adapter emits `PERSON`/`ORGANIZATION`/`LOCATION` mentions with exact provenance and rejects stopword-only capitalized sequences; the real local spaCy adapter (when bootstrapped) does the same against a real model, self-skipping (never fabricating a pass) if the model asset isn't present; a missing/unloadable model directory fails safely with `ner_runtime_unavailable`. | `uv run pytest tests/unit/structured_processing/test_ner.py` | Passing | See `docs/qa/test-results.md` |
| DOC-RELATIONS-001 | Jasraj | The four rule-based relation/event functions fire only within the documented proximity window and page scope, cite an exact combined source span, and never fire for an incident event missing a required marker/date/participant; no relation ever asserts identity, guilt, or a graph relationship. | `uv run pytest tests/unit/structured_processing/test_relations.py` | Passing | See `docs/qa/test-results.md` |
| CDR-CHUNKED-001 | Jasraj | CSV (Polars-batched), XLSX (PyArrow-batched over `openpyxl`), and JSON CDR inputs all resolve header aliases case/spacing-insensitively, preserve exact sheet/row/column/json_path provenance, reject an ambiguous schema before any row is read, and safely report (never abort on) a row-level malformed value while still processing the valid rows in the same chunk. | `uv run pytest tests/unit/structured_processing/test_chunked_processing.py tests/unit/structured_processing/test_cdr.py` | Passing | See `docs/qa/test-results.md` |
| FINANCE-CHUNKED-001 | Jasraj | Finance CSV/XLSX/JSON inputs normalize `Decimal` amounts (never `float`), flag a known-vs-unrecognized ISO 4217 currency without ever rejecting a well-formed unlisted code, normalize debit/credit direction from a documented vocabulary, resolve timestamps via the same timezone policy as CDR (optional, never aborting the record if unparseable), and never infer identity/guilt/a graph relationship. | `uv run pytest tests/unit/structured_processing/test_finance.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-DOC-BATCH-001 | Jasraj | `run_document_job_with_batches` submits one micro-batch per page/segment with real transformation provenance for every step (trust assessment, extraction/OCR, normalization, regex, NER, relations), reports a genuinely invalid PDF `FAILED` with a safe error code and zero batches submitted, and reports a page needing OCR when the OCR runtime is unavailable via a `DEFERRED` checkpoint (never a crash or fabricated success) while still submitting every other page's batch. | `uv run pytest tests/unit/structured_processing/test_worker_document_batches.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-STRUCTURED-BATCH-001 | Jasraj | `run_structured_batches_job` chunks a large CDR/finance source into bounded micro-batches with monotonically-increasing progress, renews the lease periodically, produces byte-identical deterministic batch IDs/observation IDs on an identical retry, fails outright only when every row is malformed (`partial_row_failures`), and rejects an ambiguous schema before any batch is submitted. | `uv run pytest tests/unit/structured_processing/test_worker_structured_batches.py` | Passing | See `docs/qa/test-results.md` |
| WORKER-CLIENT-BATCH-001 | Jasraj | `WorkerApiClient.submit_batch`/`.renew_lease` speak Nipun's/Aditya's exact wire format (path, `X-Claim-Token` header, request/response body shape), raise `WorkerAuthenticationError`/`WorkerApiError` on 401/409, and never log a claim token. | `uv run pytest tests/unit/structured_processing/test_worker_client.py -k "batch or renew"` | Passing | See `docs/qa/test-results.md` |
| WORKER-LIVE-BATCH-001 | Jasraj | Against a live Compose stack: a real CDR and a real finance upload each flow through claim → stream → SHA-256 verify → real chunked normalization → one or more real `/observations` micro-batches → a terminal result, exactly like the pre-existing `document`/`fir_report_text_v1` live case (extended to the same parametrized test) — `observation_count` on the user-facing job-status endpoint correctly reflects the batch-delivered observations. | `uv run pytest tests/integration/structured_processing/test_worker_live.py -k "cdr or financial"` | Passing (live) | See `docs/qa/test-results.md` |

## Coverage beyond the core matrix

- `tests/e2e/test_boot_smoke.py` — end-to-end boot + `/healthz` → `/readyz` → `/api/v1/meta/contracts` sequence against a real ASGI app instance.
- `tests/integration/test_readiness_live.py` — real connections to PostgreSQL/Neo4j/Redis/MinIO when a real `.env` and running infrastructure are present; self-skips per-dependency otherwise (never fabricates a pass).
- `tests/security/test_no_secret_leakage.py` — the generic unhandled-exception handler and `/readyz` never echo exception text or secrets, independent of any specific route.
- `tests/integration/graph/` — real Neo4j projection/query/schema/isolation checks when a real `.env` and running Neo4j are present; self-skips otherwise, mirroring `test_readiness_live.py`'s pattern exactly (see `docs/architecture/neo4j-graph-foundation.md`).
- `tests/integration/structured_processing/test_local_file_pipeline.py` — the full `process_job` pipeline across every content kind via a real local-file-backed `SourceResolver` (not `StaticBytesResolver`). No external service is needed, so this doesn't self-skip.
- `tests/integration/access_control/` — real PostgreSQL/Redis checks (migration apply, full register/login/refresh/logout round trip, refresh-token hashing, token-family revocation, live case-membership/clearance decisions, Redis-backed rate limiting) when a real `.env` and running infrastructure are present; self-skips otherwise, mirroring `test_readiness_live.py`'s pattern exactly (see `docs/architecture/access-control-v1.md`).
- `tests/security/access_control/test_module_boundaries.py` — static AST check that the access-control module never imports `app.modules.graph`/`app.modules.structured_processing` or any OCR/media/vector/ML library.
- `tests/integration/communication_processing/test_full_pipeline.py` — all seven `communication_processing` processors (`audio_metadata_v1`, `transcript_import_v1`, `diarization_import_v1`, and the four social-export profiles) succeed end to end in one pass. Needs no external service, so this doesn't self-skip.
- `tests/integration/media_processing/test_video_pipeline.py` — real `ffprobe`/`ffmpeg` against a tiny synthetic (`lavfi testsrc`-generated) MP4: probe, deterministic sample-frame extraction, the fake analysis pipeline end to end, exact frame/time/bbox provenance, repeat-processing ID stability, and temp-file cleanup. Self-skips (never fabricates a pass) if `ffmpeg`/`ffprobe` are genuinely unavailable.
- `tests/integration/media_processing/test_media_worker_live.py` — real upload → real worker `--once` claim/input/result → real graph projection → real Neo4j/HTTP confirmation, for both image and video. Self-skips whenever `.env`/the live API/PostgreSQL/Neo4j/MinIO are unavailable, mirroring `test_communication_worker_live.py`'s pattern exactly; the video case additionally self-skips if `ffmpeg`/`ffprobe` are unavailable.
- A manual real-HTTP verification script (Aditya, Phase 3; not part of the committed `pytest` suite) exercised the full worker-submission checklist against a rebuilt live Docker stack with temporarily shortened `WORKER_LEASE_SECONDS`/`WORKER_LEASE_MAX_SECONDS`/`WORKER_JOB_MAX_ATTEMPTS` (restored to normal afterward): valid claim, wrong-worker/wrong-scope denial, input access, lease renewal, expired-lease denial, reclaim, observation-batch submission, stale-token denial after reclaim, terminal result, retry exhaustion, safe audit-event persistence (including confirming no claim/worker token ever appears in any audit row), and exactly-one graph-projection-job handoff — all 25 checks passed against real PostgreSQL/Neo4j/the real HTTP API, with all created data cleaned up afterward. See `docs/qa/test-results.md` for the full dated record.

## Full suite

```bash
uv run pytest
```
# Phase 5 graph/correlation integration

| ID | Scenario | Coverage |
|---|---|---|
| GRAPH-CORR-001 | Atomic correlation/candidate/snapshot + graph-update event persistence | `test_phase5_correlation_integration_live.py` |
| GRAPH-CORR-002 | Duplicate submission and deterministic replay identity | unit projector + live integration test |
| GRAPH-CORR-003 | Neo4j outage leaves durable event retryable; later replay succeeds | unit projector + live integration test |
| GRAPH-CORR-004 | Missing referenced correlation fails with a safe fixed error code | `test_integration_projector.py` |
| GRAPH-CORR-005 | Cross-case observation references are rejected | live integration test |
| GRAPH-CORR-006 | Candidate-only API semantics and case-scoped safe responses | `test_integration_api.py`, `test_integration_models.py` |
| GRAPH-INTEL-001 | Exact identifiers, aliases, transliterations, local vectors, and bounded time are deterministic and case-scoped candidates | `tests/unit/graph/test_intelligence.py` |
| GRAPH-INTEL-002 | Rules snapshots retain named contributions and candidate-only status through Nipun's typed submission seam | `tests/unit/graph/test_intelligence.py` |
| GRAPH-INTEL-003 | The semantic handler requires the projected Evidence → Observation chain and Nipun projection key | `tests/unit/graph/test_intelligence_projection.py` |
| GRAPH-INTEL-004 | PageRank, betweenness, WCC, community baseline, and temporal motif operate on one synthetic case | `tests/unit/graph/test_intelligence.py` |
| GRAPH-INTEL-005 | Phase 5A reconciliation: `sourcing.descriptor_from_observation` maps real Jasraj/Sarthak observation shapes (FIR phone/email/vehicle/account mentions, CDR/finance combined records, chat messages + transliteration) into retrieval input with correct, documented, bounded scope; unmapped types are skipped, never guessed at; raw message text never leaks into the derived descriptor | `tests/unit/graph/test_intelligence_sourcing.py` |
| GRAPH-INTEL-006 | `intelligence/pipeline.py::build_case_correlation_submission` rejects any observation outside the requested case; identical observations produce an identical, deterministic submission/hash; a changed observation changes the idempotency key; too few/unrelated observations return `None`, never an error; submission status is always candidate/needs-review, never a verified value | `tests/unit/graph/test_intelligence_pipeline.py` |
| GRAPH-INTEL-007 | `PgvectorCandidateStore` raises `VectorSearchUnavailable` on a DBAPI error (never a silent fallback), rejects an out-of-bounds search limit before touching the engine, and computes deterministic vector IDs/hashes; a live test confirms the pgvector extension is genuinely installed and exercises real upsert/search/case-isolation/idempotency | `tests/unit/graph/test_intelligence_vector_store.py`, `tests/integration/graph/test_intelligence_vector_store_live.py` |
| GRAPH-INTEL-008 | Full live pipeline: a real correlation pass reaches Nipun's durable outbox exactly once; replaying the entire pipeline (submission + Neo4j replay) twice never duplicates a correlation/candidate/snapshot/event/graph node; the semantic handler creates no `Correlation` node when the required Evidence→Observation provenance was never projected; analytics/motif snapshots are deterministic and case-scoped for the same seed | `tests/integration/graph/test_intelligence_pipeline_live.py` |
| GRAPH-INTEL-009 | `app/modules/graph/intelligence_worker.py` composes the registered semantic handler with `replay_graph_updates` and exposes `--replay-once`/`--replay-loop`/`--generate --case-id` — closing the "handler exists but nothing invokes it outside a test" gap the Phase 5A audit found | CLI smoke-tested directly (`--help`, missing-arg validation); full behavior covered transitively by GRAPH-INTEL-008's live test, which calls the same underlying functions |
| VISUAL-SIGNAL-005B | Image/video locators, normalized geometry, deterministic frame-time mapping, chunk bounds, evidence-local tracks, bounded OCR provenance, and accepted/rejected/incomplete source quality are validated before a visual signal is marked correlation-ready. | `tests/unit/media_processing/test_visual_validation.py`, `test_media_worker.py`, `test_ocr_batching.py`, `test_iou_tracker.py` |
| COMM-SIGNAL-005B | Transcript/diarization time and supplied-chunk bounds, local-speaker labels, safe ASR/backend metadata, chat platform/locator/sender/time validation, platform-scoped handles, deterministic Unicode normalization, safe SHA-256 text commitments, and Phase-5 correlation-readiness gating are covered with synthetic fixtures only. | `tests/unit/communication_processing/test_signal_validation.py`, `test_transcript_import.py`, `test_diarization_import.py`, `test_communication_worker_orchestration.py` |
| GRAPH-READ-ABAC-005B | Every exposed Phase 5 graph/correlation/candidate/hypothesis read rejects unauthenticated and cross-case calls before the data boundary; a valid correlation UUID from another case is non-retrievable; allow/deny audit metadata contains only the action and identifiers. | `tests/unit/graph/test_graph_api.py`, `tests/unit/graph/test_integration_api.py`, `tests/unit/access_control/test_case_access_audit.py` |
| GRAPH-READ-OUTAGE-005B | PostgreSQL correlation-read and authorization-store failures return fixed 503 errors without query text, DSNs, credentials, or source content. | `tests/unit/graph/test_graph_api.py`, `tests/unit/access_control/test_case_access_audit.py` |
| GRAPH-VECTOR-SCOPE-005B | pgvector search binds the descriptor's case ID before ordering/returning candidates. | `tests/unit/graph/test_intelligence.py` |
| PHASE4-WORKER-SECURITY-001 | TLS-required worker control routes reject insecure transport, accept forwarded HTTPS only from a configured proxy, reject declared oversized metadata payloads, and preserve explicit local HTTP configuration. | `tests/unit/evidence_lifecycle/test_worker_transport_security.py` |
| PHASE4-MEDIA-GRAPH-001 | Synthetic canonical visual, audio, chat, and explicit meeting-candidate observations map deterministically to case-scoped temporal events with source locator, precision, provenance, and optional safe staged lineage. Labels/handles stay unresolved; source content is excluded. | `tests/unit/graph/test_mapping.py`, `tests/unit/graph/test_phase_4_media_mapping.py`, `tests/unit/graph/test_projector.py` |
| PHASE4-COMM-EXEC-001 | Explicit fake local ASR/diarization adapters are invoked through the real claimed worker path; raw-audio model absence is deferred; exact source-relative timing, profile/manifest/chunk/model hashes, source-local speaker labels, and transcript exclusion are retained. | `tests/unit/communication_processing/test_communication_worker_orchestration.py`, `test_asr_adapter.py`, `test_diarization_adapter.py` |
| P5-REGRESSION-AUTH-001 | An authenticated client presenting its newest rotated refresh token on every call still hits `429` once it exceeds the configured limit (previously unreachable — each rotated token landed in its own one-shot rate-limit bucket); an invalid/unauthenticated attempt still safely `401`s until its own bucket is exhausted; a different caller's bucket is unaffected. | `tests/unit/access_control/test_service.py`, `test_api.py` |
| P5-INTEG-VISUAL-001 | A video worker registers a coordinator-persisted manifest (`POST .../media-manifest`) before publishing; every sampled frame's OCR output is combined per assigned chunk and published exactly once via `POST .../media-chunks/publish`; an observation whose interval falls outside its declared chunk, or spans a chunk boundary, is rejected `422` by the coordinator independent of the worker's own claim. | `tests/unit/evidence_lifecycle/test_media_orchestration.py`, `tests/unit/media_processing/test_media_worker_orchestration.py`, `tests/integration/media_processing/test_media_worker_live.py`, `tests/integration/graph/test_phase5_final_acceptance_live.py` |
| P5-INTEG-COMMUNICATION-001 | The local-audio worker registers a coordinator-persisted manifest before `_handle_local_audio` runs; ASR/diarization mentions (already chunk-tagged since Phase 4) are grouped by chunk and published via the same chunk-scoped route; the whole-file audio-metadata mention and every other communication profile are unaffected. | `tests/unit/communication_processing/test_communication_worker_orchestration.py` |
| P5-SOURCING-GATES-001 | A structured (CDR/finance) observation with a present, non-`"accepted"` `source_signal_quality.outcome` never reaches descriptor/correlation or motif input; a document mention with no such attribute at all (legacy-compatible) is unaffected; a mixed batch of structured/visual/communication observations applies each family's own gate independently. | `tests/unit/graph/test_intelligence_sourcing.py` |
| P5-TWO-PARTY-001 | A `cdr_call_record`/`financial_transaction_record` yields both parties as independent, role-scoped, deterministically-identified descriptors sharing one origin observation; the two ends of one record never become a candidate of each other (same-event suppression); a number/account seen as one role on one record and the other role on a different record still collides (role-blind cross-observation blocking); descriptor IDs are stable/reproducible across repeated calls. | `tests/unit/graph/test_intelligence_sourcing.py` |
| P5-RULES-BASELINE-001 | The frozen `phase5_preliminary_rules_v1` baseline achieves Precision@K = 1.0/Recall@K = 1.0/0 false links on a versioned synthetic benchmark; same-event pairs never score; a genuine contradiction is recorded and downgrades a candidate to `needs_review` without discarding its match signal; a cross-case comparison attempt is rejected; retrieval/scoring output is deterministic across repeated runs; one explicit alternative rules-weight configuration is compared and does not change which candidates are retrieved or their ranking. | `tests/unit/graph/test_phase5_rules_benchmark.py` |
| P5-ACCEPTANCE-001 | Full live flow: case/evidence context → worker claim → manifest registration → chunk-scoped OCR publication (plus a rejected out-of-scope interval) → validated two-party CDR observations (plus one rejected decoy) and a chat observation → correlation pass → idempotent replay → authorized case-scoped read → denied cross-membership read → raw chat-body exclusion with OCR-text-as-legitimate-mention distinguished. | `tests/integration/graph/test_phase5_final_acceptance_live.py` |

## Phase 6 Part 1 — integrity foundation (Nipun)

The 18 required proof points from the Phase 6 Part 1 task spec, mapped to
their tests. Pure/logic proof points need no live infrastructure; DB-backed
proof points self-skip (never fabricate a pass) without a reachable
PostgreSQL, mirroring every other `tests/integration/*` suite.

| ID | Proof point | Coverage |
|---|---|---|
| INTEGRITY-001 | Canonical leaf hashes and Merkle roots are deterministic for identical input | `tests/unit/integrity/test_hashing.py` |
| INTEGRITY-002 | Dictionary insertion order cannot change a leaf/metadata hash | `tests/unit/integrity/test_hashing.py::test_canonical_metadata_hash_is_independent_of_dict_insertion_order` |
| INTEGRITY-003 | Different cases never share a checkpoint | `tests/integration/integrity/test_repository_live.py::test_checkpoints_never_cross_cases` |
| INTEGRITY-004 | Event sequence number (not wall-clock order) determines leaf order | `tests/unit/integrity/test_hashing.py::test_root_depends_on_leaf_order_not_only_leaf_set`, `tests/integration/integrity/test_repository_live.py::test_event_sequence_determines_leaf_order_not_wall_clock` |
| INTEGRITY-005 | Odd-leaf behavior follows the frozen duplicate-last rule | `tests/unit/integrity/test_hashing.py::test_odd_leaf_count_duplicates_the_final_leaf` |
| INTEGRITY-006 | Exact integrity-event retry is idempotent | `tests/integration/integrity/test_repository_live.py::test_exact_retry_is_idempotent` |
| INTEGRITY-007 | Conflicting idempotency-key reuse is safely rejected | `tests/integration/integrity/test_repository_live.py::test_conflicting_idempotency_key_reuse_is_rejected` |
| INTEGRITY-008 | Identical checkpoint-range retry is idempotent | `tests/integration/integrity/test_repository_live.py::test_checkpoint_build_is_idempotent_for_an_exact_range` |
| INTEGRITY-009 | Invalid overlapping/inconsistent checkpoint creation is rejected | `tests/integration/integrity/test_repository_live.py::test_overlapping_checkpoint_range_is_rejected` |
| INTEGRITY-010 | A direct update of an integrity event is rejected by PostgreSQL | `tests/integration/integrity/test_repository_live.py::test_database_rejects_update_on_integrity_event` |
| INTEGRITY-011 | A direct delete of an integrity event is rejected by PostgreSQL | `tests/integration/integrity/test_repository_live.py::test_database_rejects_delete_on_integrity_event` |
| INTEGRITY-012 | Swapped leaf order is detected | `tests/unit/integrity/test_hashing.py::test_root_depends_on_leaf_order_not_only_leaf_set` |
| INTEGRITY-013 | Direct checkpoint/signature update and delete are rejected by PostgreSQL | `tests/integration/integrity/test_repository_live.py::test_database_rejects_checkpoint_and_signature_mutation` |
| INTEGRITY-014 | A valid Ed25519 signature verifies | `tests/unit/integrity/test_signing.py::test_valid_signature_verifies`, `tests/integration/integrity/test_repository_live.py::test_full_build_verify_export_round_trip_succeeds` |
| INTEGRITY-015 | A wrong public key or altered signature/root fails verification | `tests/unit/integrity/test_signing.py::test_wrong_public_key_fails_verification`, `test_altered_signature_fails_verification`, `test_altered_root_fails_verification` |
| INTEGRITY-016 | Private key material and raw sensitive content never appear in stored records, exported bundles, logs, or CLI output | `tests/unit/integrity/test_signing.py::test_signed_root_never_carries_private_key_material`, `tests/unit/integrity/test_models.py` (forbidden-metadata rejection), `tests/integration/integrity/test_repository_live.py::test_full_build_verify_export_round_trip_succeeds` (bundle-content scan) |
| INTEGRITY-017 | Existing evidence/observation/correlation behavior remains fully backward compatible | Full pre-existing `tests/unit/evidence_lifecycle` + `tests/unit/graph` suite (386 tests, unaffected); `tests/integration/evidence_lifecycle/test_integrity_producer_seam_live.py` |
| INTEGRITY-018 | Migration upgrade succeeds; exactly one Alembic head | `tests/unit/integrity/test_migration_head.py`; live upgrade exercised by `tests/integration/integrity/conftest.py`'s `_migrated_database` fixture |
| INTEGRITY-019 | Protected checkpoint list/detail/verify/export applies independent integrity actions, generic 401/403 behavior, cross-case isolation, and safe bundle fields | `tests/unit/integrity/test_integrity_api.py` |
| INTEGRITY-020 | Direct SQL update/delete attempts against integrity events, checkpoints, and signatures fail; ordinary inserts still succeed | `tests/integration/integrity/test_repository_live.py` (requires an already-running PostgreSQL) |
| INTEGRITY-021 | Document/OCR/CDR/finance observations yield deterministic safe structured-provenance commitments; retries replay and changed commitments differ | `tests/unit/integrity/test_structured_provenance.py` |
| INTEGRITY-022 | Structured-provenance reconciliation replays only an exact, case-scoped safe projection | `tests/unit/integrity/test_reconciliation.py` |
| INTEGRITY-023 | Visual/communication projections are accepted-only, deterministic, commitment-only, and preserve local non-identity semantics | `tests/unit/integrity/test_modality_provenance.py` |
| INTEGRITY-024 | Modality provenance migration remains on one Alembic head | `tests/unit/integrity/test_migration_head.py` |

## Phase 6 Part 5 — review and hypothesis workflow, final release gate (Shreshtha)

| ID | Proof point | Coverage |
|---|---|---|
| REVIEW-001 | Effective candidate review status (`needs_review`/`accepted_by_reviewer`/`rejected_by_reviewer`) is computed at read time, never a stored mutation of Nipun's `PropositionStatus` | `tests/unit/graph/test_review_models.py` |
| REVIEW-002 | A review decision's rationale commitment is deterministic and never the raw text; the integrity submission carries only the commitment | `tests/unit/graph/test_review_models.py` |
| REVIEW-003 | Candidate-not-found raises before any write; a conflicting review decision propagates and never overwrites; an exact-replay decision never re-fires integrity or projection | `tests/unit/graph/test_review_service.py` |
| REVIEW-004 | An integrity-recording or Neo4j-projection failure never blocks an already-committed review decision | `tests/unit/graph/test_review_service.py` |
| REVIEW-005 | Candidate review projection `MATCH`es (never `MERGE`s) the existing `Correlation` node and is a safe no-op when the correlation was never projected | `tests/unit/graph/test_review_projection.py` |
| HYPOTHESIS-001 | A hypothesis must cite at least one case-scoped observation or candidate; duplicate references are rejected; statement/rationale are bounded | `tests/unit/graph/test_hypothesis_models.py` |
| HYPOTHESIS-002 | `HypothesisStatus` never contains a confirmed/true value; safe metadata and the `hypothesis_action` integrity submission never carry raw statement/rationale text | `tests/unit/graph/test_hypothesis_models.py` |
| HYPOTHESIS-003 | The review idempotency key is fixed regardless of outcome, so a second conflicting review decision is rejected, never applied | `tests/unit/graph/test_hypothesis_models.py` |
| HYPOTHESIS-004 | A missing hypothesis returns `None` (rendered `404`); a replayed creation/review never re-fires integrity | `tests/unit/graph/test_review_service.py` |
| HYPOTHESIS-005 | Hypothesis Neo4j projection is provenance-gated exactly like `Correlation`'s; a hypothesis with no evidence paths is a safe no-op; a candidate-reference edge uses `MATCH` on both sides | `tests/unit/graph/test_review_projection.py` |
| ACCESS-HYPOTHESIS-001 | `HYPOTHESIS_PROPOSE` is granted to owner/manager/investigator only; hypothesis review reuses the existing `REVIEW_DECIDE` grant | `tests/unit/access_control/test_policy.py` |
| PHASE6-LIVE-001 | Full live workflow: authorization ordering (401 before lookup, 403 for cross-case/non-member/wrong-role, 404 for a missing candidate/hypothesis, no existence leakage), a real candidate review decision reaching the durable table + integrity event + provenance-gated `Correlation` projection, idempotent retry, conflicting-retry `409`, a real hypothesis citing a real observation and candidate reaching a provenance-gated `Hypothesis` node with a `REFERENCES_CANDIDATE` edge, direct PostgreSQL `UPDATE`/`DELETE` rejected on both new append-only tables, and reconciliation replay for both new leaf kinds | `tests/integration/graph/test_review_and_hypothesis_live.py` |

## Phase 7 Part 1 — evaluation foundation and local-model governance (Nipun)

The 17 required proof points from the Phase 7 Part 1 task spec, mapped to
their tests. Every test in this section is a pure unit/contract test --
none needs live PostgreSQL/Neo4j/Redis/MinIO, since Part 1 is a
configuration/contract-only part with no downloaded dataset, no downloaded
model, and no database write of its own.

| ID | Proof point | Coverage |
|---|---|---|
| EVAL-001 | Every Dataset Manifest V1 entry parses | `tests/unit/evaluation/test_dataset_manifest.py::test_every_dataset_manifest_entry_parses` |
| EVAL-002 | Unsupported schema version is rejected | `tests/unit/evaluation/test_models.py::test_unsupported_schema_version_is_rejected` |
| EVAL-003 | Invalid dataset role/owner/storage policy is rejected | `tests/unit/evaluation/test_models.py::test_invalid_role_is_rejected`, `test_invalid_owner_is_rejected`, `test_invalid_storage_policy_is_rejected` |
| EVAL-004 | Unsafe local path values are rejected | `tests/unit/evaluation/test_models.py::test_absolute_local_path_is_rejected`, `test_home_relative_local_path_is_rejected`, `test_traversal_local_path_is_rejected`, `test_local_path_outside_allowed_roots_is_rejected` |
| EVAL-005 | Every primary dataset has allowed tasks, prohibited claims, and limitations | `tests/unit/evaluation/test_dataset_manifest.py::test_every_primary_dataset_declares_tasks_claims_and_limitations` |
| EVAL-006 | Operation Nightfall is protected as private showcase holdout | `tests/unit/evaluation/test_dataset_manifest.py::test_operation_nightfall_is_protected_as_private_showcase_holdout`, `test_manifest_rejects_operation_nightfall_with_the_wrong_role` |
| EVAL-007 | Public benchmark datasets cannot be marked as one real investigation | `tests/unit/evaluation/test_dataset_manifest.py::test_public_benchmark_datasets_cannot_be_marked_as_one_real_investigation` |
| EVAL-008 | Model candidates cannot start as `selected` | `tests/unit/evaluation/test_catalog.py::test_model_candidate_cannot_start_as_selected` |
| EVAL-009 | Completed successful benchmark runs require an artifact SHA-256 | `tests/unit/evaluation/test_results.py::test_completed_successful_run_requires_artifact_sha256` |
| EVAL-010 | Result metrics reject unsafe raw-content fields | `tests/unit/evaluation/test_results.py::test_result_metrics_reject_unsafe_raw_content_via_forbidden_key`, `test_result_metrics_reject_a_raw_string_value`, `test_failure_reason_rejects_a_leaked_credential` |
| EVAL-011 | All required metric groups are represented | `tests/unit/evaluation/test_results.py::test_all_required_metric_groups_are_represented` |
| EVAL-012 | Case-level split rejects the same case in two groups | `tests/unit/evaluation/test_splits.py::test_case_level_split_rejects_the_same_case_in_two_groups` |
| EVAL-013 | Case-plan count outside 12-15 is rejected | `tests/unit/evaluation/test_splits.py::test_case_plan_count_outside_12_to_15_is_rejected`, `test_case_plan_total_cases_must_match_its_own_declared_bounds` |
| EVAL-014 | Operation Nightfall cannot be put in development/tuning | `tests/unit/evaluation/test_splits.py::test_operation_nightfall_cannot_appear_in_a_tunable_case_group`, `test_operation_nightfall_cannot_appear_in_holdout_either` |
| EVAL-015 | The two investigator-report proof requirements are represented | `tests/unit/evaluation/test_models.py::test_both_investigator_report_requirements_are_represented`, `test_investigator_lead_report_must_prohibit_guilt_language` |
| EVAL-016 | `.gitignore` contains targeted protections for data/model/private-truth paths | `tests/unit/evaluation/test_dataset_manifest.py::test_gitignore_has_targeted_evaluation_data_safety_rules` |
| EVAL-017 | Existing regression tests remain unaffected | Full pre-existing repository suite (see `docs/qa/test-results.md`'s dated Phase 7 Part 1 entry) -- Part 1 adds only new, additive files; nothing existing was edited except `.gitignore` (additive) |

## Phase 7 Part 2 — structured-data and local OCR benchmarking (Jasraj)

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| BENCH-VALIDATION-001 | Jasraj | Only the four approved Part 2 dataset/candidate pairs are accepted; every other structurally-valid Part 1 dataset/candidate ID (owned by another Part) is rejected safely; local roots resolve only from `TRACEX_BENCHMARK_DATA_ROOT`/`TRACEX_MODEL_CACHE_ROOT`/`TRACEX_BENCHMARK_OUTPUT_ROOT` or an explicit CLI flag, never a hardcoded path; a private-looking path (`/home/...`, `/Users/...`, `~/...`) is rejected from a result payload; the inference-config hash changes with the configuration and is otherwise deterministic. | `uv run pytest tests/unit/structured_processing/test_benchmark_safety.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-OCR-001 | Jasraj | `FakeOcrEngine` produces a valid, typed `BenchmarkRunV1` for a document/FIR OCR run; a per-document failure (unreadable/unsupported format/execution failure) is safely categorized without leaking file content; a run where every document fails reports `FAILED`, never a fabricated `SUCCEEDED`; the aggregate result never contains raw OCR text, reference text, or a raw error message; `artifact_sha256` is required for a `SUCCEEDED` OCR run. | `uv run pytest tests/unit/structured_processing/test_benchmark_adapters.py -k ocr` | Passing | See `docs/qa/test-results.md` |
| BENCH-STRUCTURED-001 | Jasraj | The CDR/finance benchmark adapter reuses `structured.chunked_processing.assess_schema`/`normalize_chunk` unchanged; malformed rows are safely categorized and never inflate `accepted_row_count`/`mentions_emitted_count`; a file where every row fails normalization reports `FAILED` (mirroring `worker.run_structured_batches_job`'s own policy); the aggregate result never contains a raw phone number, caller/callee value, account number, amount, currency narration, or transaction reference. | `uv run pytest tests/unit/structured_processing/test_benchmark_adapters.py -k "cdr or finance or structured"` | Passing | See `docs/qa/test-results.md` |
| BENCH-CLI-001 | Jasraj | `benchmark_cli.main` rejects an unknown dataset/candidate ID with exit code 2 and no stack trace; reports a missing local artifact as `UNAVAILABLE` with exit code 1; succeeds with exit code 0 and writes one safe result JSON file to the configured output root for a real local CDR/finance input; `--help` documents every local-root flag; no result JSON or stdout output ever contains a raw fixture value. | `uv run pytest tests/unit/structured_processing/test_benchmark_cli.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-SAFETY-001 | Jasraj | The benchmark module never imports Neo4j (static source check); `run_benchmark` never writes a local filesystem root into its own result; accepted benchmark output retains canonical `ObservationV1`/provenance compatibility; a benchmark failure never leaks source content or a raw stack trace; every result field is safe to commit (aggregate metadata only). | `uv run pytest tests/unit/structured_processing/test_benchmark_safety.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-SMOKE-001 | Jasraj | Against a real local `TRACEX_BENCHMARK_DATA_ROOT` (self-skips, never fabricating a pass, when unset or the dataset directory is absent/empty): the real `benchmark_cli` subprocess runs end to end for `gomask_voice_cdr`/`ibm_amlsim`/`fir_icdar_2023`, producing exactly one safe result JSON file per run. | `uv run pytest tests/integration/structured_processing/test_local_benchmark_smoke.py` | Passing (self-skips without local artifacts, as expected on this development machine) | See `docs/qa/test-results.md` |

## Phase 7 Part 3 — visual benchmark foundation and local-model governance (Gaurav)

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| BENCH-VISUAL-METRICS-001 | Gaurav | Detection precision/recall/mAP are computed correctly via IoU matching when ground truth is present, and are `None` when it is absent; tracking IDF1/MOTA/ID-switches are computed correctly against ground-truth tracks and correctly detect an identity switch; HOTA is always `None`, never a fabricated approximation. | `uv run pytest tests/unit/media_processing/test_visual_benchmark_metrics.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-VISUAL-ADAPTERS-001 | Gaurav | `FakeDetectorEngine`/`FakeTrackerEngine`/`FakeVisualTextEngine` produce valid, typed `BenchmarkRunV1` results through the same run functions a real engine would use; an empty sample list is rejected; a per-sample engine failure is safely categorized without leaking content; a run where every sample fails reports `FAILED`, never a fabricated `SUCCEEDED`; visual-text metrics never contain the raw recognized/reference text; an accepted detection/track/OCR result builds a valid canonical `ObservationV1` through the exact, unmodified `provenance.build_extractor`/`draft_to_observation` seam; a technical track ID never becomes an `ExtractedEntityMention` or any other identity-shaped field. | `uv run pytest tests/unit/media_processing/test_visual_benchmark_adapters.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-VISUAL-SAFETY-001 | Gaurav | Only the approved Part 3 dataset/candidate/pair combinations are accepted; a dataset or candidate whose `license_status` is `pending_verification` blocks a real run (`UNAVAILABLE`, never `SUCCEEDED`) -- still true for `safe_unsafe_behaviour`/`ufpr_alpr`, while `virat_ground`/`yolo11n`/`yolo11s`/`bytetrack` are now genuinely `verified_restricted_noncommercial` and clear the gate; a `SUCCEEDED` result without `artifact_sha256` is rejected by the frozen `BenchmarkRunV1` validator; a metric key shaped like a real identifier is rejected; a private local filesystem path is rejected from a result payload; the inference-config hash is deterministic and content-sensitive; none of the five new modules imports a database/object-store client; the real Ultralytics `BYTETracker` wiring genuinely builds and tracks a real object across frames when `ultralytics` is installed (self-skips otherwise). | `uv run pytest tests/unit/media_processing/test_visual_benchmark_safety.py` | Passing (43, was 37) | See `docs/qa/test-results.md` |
| BENCH-VISUAL-CLI-001 | Gaurav | `visual_benchmark_cli`'s `list-candidates`/`validate`/`run` subcommands work end to end against isolated `tmp_path` roots; an unknown dataset/candidate or unsupported pair is rejected with exit code 2; a missing dataset directory or unresolved licence produces a safe `unavailable` result with exit code 1 and a written result file containing no local path; the now-cleared `virat_ground`/`yolo11n` pair is confirmed reachable through the CLI. | `uv run pytest tests/unit/media_processing/test_visual_benchmark_cli.py` | Passing (7, was 6) | See `docs/qa/test-results.md` |
| BENCH-VISUAL-SMOKE-001 | Gaurav | Against a real local `TRACEX_BENCHMARK_DATA_ROOT`: the real `visual_benchmark_cli` subprocess genuinely runs end to end for `virat_ground` detection and tracking against a real downloaded clip, accepting any safe outcome (`succeeded`/`failed`/`unavailable`) without crashing; the `ufpr_alpr` visual-text case still self-skips (never fabricating a pass), since that dataset remains deferred. A genuine `succeeded` status for `virat_ground` detection/tracking is independently confirmed by the direct CLI runs recorded in `docs/qa/test-results.md`'s dated Gate B entry. | `uv run pytest tests/integration/media_processing/test_visual_benchmark_smoke.py` | Passing (2 passed against real data, 1 skipped) | See `docs/qa/test-results.md` |
| BENCH-VISUAL-EXEMPTION-001 | Gaurav | The pre-existing `media_processing`-wide "no infra/ML library" static check excludes `visual_benchmark*.py` for `ultralytics`/`paddleocr` only -- every other forbidden library (databases, object storage, queues, `torch`/`tensorflow`/`sklearn`) remains forbidden in the benchmark harness too, and the exemption does not weaken the check for any production file. | `uv run pytest tests/unit/media_processing/test_media_safety.py` | Passing | See `docs/qa/test-results.md` |

## Phase 7 Part 4 — audio and social/chat benchmark foundation and local-model governance (Sarthak)

| ID | Owner | Expected behavior | Test command | Status | Result |
|---|---|---|---|---|---|
| BENCH-AUDIO-SOCIAL-METRICS-001 | Sarthak | ASR WER/CER, VAD precision/recall/F1/accuracy, diarization DER (majority-vote relabeling correctly handled) and speaker-turn-quality/speaker-count-error, language-identification accuracy, and social-extraction precision/recall/F1 are computed correctly on valid synthetic data; JER is always `None`, never a fabricated approximation; every metric is `None` (not fabricated) without ground truth or on a length mismatch. | `uv run pytest tests/unit/communication_processing/test_audio_social_benchmark_metrics.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-AUDIO-SOCIAL-ADAPTERS-001 | Sarthak | `Fake*Engine`s for ASR/VAD/diarization/language-ID/social-extraction, and the real `DeterministicSocialExtractionEngine` (which reuses the unmodified `social/identifiers.py::extract_mentioned_identifiers`), all produce valid, interchangeable, typed `BenchmarkRunV1` results; an empty sample list is rejected; a per-sample engine failure is safely categorized without leaking content; a run where every sample fails reports `FAILED`; social-extraction metrics never contain the raw message text; an accepted ASR/diarization/social result builds a valid canonical `ObservationV1` through the exact, unmodified production seam; a recording-local speaker label becomes only an `entity_type_hint="speaker_label_local"` mention, never a resolvable cross-record identity. | `uv run pytest tests/unit/communication_processing/test_audio_social_benchmark_adapters.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-AUDIO-SOCIAL-SAFETY-001 | Sarthak | Only the approved Part 4 dataset/candidate/pair combinations are accepted; a dataset or candidate whose `license_status` is unresolved, or a candidate whose `selection_status` is `conditional`, blocks a real run (`UNAVAILABLE`, never `SUCCEEDED`) -- true for every real ASR/VAD/diarization pair today, while `vast_social_text`/`vast_2014_mixed_records` + `existing-deterministic-social-parsers` are already cleared; a `SUCCEEDED` result without `artifact_sha256` is rejected by the frozen `BenchmarkRunV1` validator; a metric key shaped like a real identifier is rejected; a private local filesystem path is rejected from a result payload; the inference-config hash is deterministic and content-sensitive; a missing optional dependency (faster-whisper/pyannote.audio/fastText/torch, including fastText's own chained faster-whisper transcription stage) produces a safe `UNAVAILABLE`, never a crash; none of the five new modules imports a database/object-store client. **Real VAD wiring never calls `torch.hub.load` against a remote GitHub source** (proven via a fake `torch.hub.load` that asserts `source="local"` and a non-GitHub `repo_or_dir`); a missing local Silero snapshot, a snapshot path that escapes the configured model cache root (via a symlink), or a weight-file SHA-256 mismatch are all blocked safely; a valid local snapshot with a matching hash reaches the adapter seam with no network access. **A successful language-ID result requires both the `fasttext-lid176` artifact and its chained transcription-stage artifact** -- an absent transcription-stage reference is rejected before any engine is built, and the internal transcript text is proven to never leak into the returned result. | `uv run pytest tests/unit/communication_processing/test_audio_social_benchmark_safety.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-AUDIO-SOCIAL-CLI-001 | Sarthak | `audio_social_benchmark_cli`'s `list-candidates`/`validate`/`run` subcommands work end to end against isolated `tmp_path` roots; an unknown dataset/candidate or unsupported pair is rejected with exit code 2; a missing dataset directory or unresolved licence produces a safe `unavailable` result with exit code 1 and a written result file containing no local path; the already-cleared social-extraction pair is confirmed reachable through the CLI; the `--transcription-stage-model-name`/`-version`/`-sha256` flags `fasttext-lid176` requires are accepted and threaded through `run_benchmark` without error. | `uv run pytest tests/unit/communication_processing/test_audio_social_benchmark_cli.py` | Passing | See `docs/qa/test-results.md` |
| BENCH-AUDIO-SOCIAL-SMOKE-001 | Sarthak | Against a real local `TRACEX_BENCHMARK_DATA_ROOT` (self-skips, never fabricating a pass, when unset or the dataset directory is absent/empty): the real `audio_social_benchmark_cli` subprocess runs end to end for `common_voice_indic` (ASR), `ami_meeting_corpus` (diarization), and `vast_social_text` (social extraction) -- the last of which reaches a genuine `succeeded` result against a synthetic local dataset directory, confirmed during this task's own verification. | `uv run pytest tests/integration/communication_processing/test_audio_social_benchmark_smoke.py` | Passing (self-skips without local artifacts, as expected on this development machine) | See `docs/qa/test-results.md` |
| BENCH-AUDIO-SOCIAL-EXEMPTION-001 | Sarthak | The pre-existing `communication_processing`-wide "no infra/ML library" static check excludes `audio_social_benchmark*.py` for `faster_whisper`/`pyannote`/`fasttext`/`torch` (and separately, `os`, for local-root resolution) only -- every other forbidden library (databases, object storage, queues, `subprocess`, `transformers`/`whisper`/`speechbrain`/`librosa`/`sklearn`/`numpy`) remains forbidden in the benchmark harness too, and the exemption does not weaken the check for any production file. | `uv run pytest tests/unit/communication_processing/test_module_safety.py` | Passing | See `docs/qa/test-results.md` |
